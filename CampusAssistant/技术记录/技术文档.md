# 技术文档

## 2026-07-20 首页重构 + 底部Tab改造

实现了首页按 preview-ui.html 原型重构，包括 Banner 卡片、快捷入口网格、失物招领大卡片、今日课表+我的报名并排布局、校园公告列表，通过 ArkUI Tabs + linearGradient + Flex 布局技术实现。课表从底部Tab移至首页快捷入口，新增独立 SchedulePage 子页面，通过 router.pushUrl 跳转。底部Tab改为首页/二手/消息/AI/我的 五栏布局，新增消息Tab的 ChatListContent 对话列表。

遇到了编辑替换后多余闭合大括号导致编译报错的问题，用 awk 检查大括号平衡性后定位并修复方法解决。

## 2026-07-20 快捷入口布局调整

实现了快捷入口2x2网格+失物招领等宽并排布局，课表/空教室/社团互动/AI助手左半区2行2列，失物招领右半区等宽渐变卡片，通过 Row + Column layoutWeight(1) 双栏均分布局技术实现。

## 2026-07-22 修复消息Tab跳转空白页Bug

实现了消息Tab点击直接跳转ConversationListPage且返回不回到空白页，通过在Tabs.onChange中拦截index=2阻止tab切换并直接pushUrl跳转，同时在onPageShow中检测currentTab为2时重置回previousTab技术实现。

遇到了点击消息tab先显示空白MessageContent再跳转导致体验不丝滑、以及从ConversationListPage返回后currentTab仍为2显示空白页的问题，用记录previousTab并在onChange拦截+onPageShow重置方法解决。遇到了仅修改currentTab无法阻止Tabs视觉切换导致底部tab栏消失的问题，用TabsController.changeIndex强制切回previousTab方法解决。

## 2026-07-23 消息Tab内嵌会话列表

实现了消息Tab直接内嵌会话列表，点击底部tab即可查看会话无需页面跳转，通过将ConversationListPage的会话加载、WebSocket消息处理、在线状态管理逻辑整合到MainPage的MessageContent Builder中技术实现。移除了之前的onChange拦截+pushUrl跳转方案，改为正常切换tab时懒加载会话数据。

## 2026-07-23 全局导航栏统一为NavHeader

实现了13个页面的自定义导航栏统一替换为NavHeader组件，通过NavHeader的title属性和@BuilderParam rightSlot技术实现，右侧操作按钮（发布/刷新/清空/保存/删除等）放入rightSlot。ChatPage的在线状态和商品号信息从导航栏移至NavHeader下方独立Row展示。

## 2026-07-23 头像显示Bug修复

实现了编辑资料页头像即时更新和其他页面头像同步，通过将AvatarView的avatar和name改为@Prop使其响应式更新、onPageShow中创建新UserInfo对象触发@State变化检测技术实现。修复了AvatarView图片URL判断仅支持/开头的逻辑，增加http/https前缀支持。更换了编辑资料页emoji列表为更适合做头像的动物和自然类emoji。

遇到了@State赋值同一对象引用不会触发UI刷新的问题，用创建新对象浅拷贝方法解决。

## 2026-07-27 答辩PPT样式预览居中修复

实现了答辩PPT三个HTML样式预览页面在浏览器视口中的居中显示，通过将固定1920×1080舞台改为absolute定位到视口中心并使用translate(-50%, -50%) scale(...)组合变换技术实现。

遇到了预览页面在部分浏览器窗口中显示偏移的问题，用中心锚点定位替代grid居中缩放方法解决。

## 2026-07-28 答辩PPT活力风格预览生成

实现了面向学生用户的三套答辩PPT活力风格HTML封面预览，通过固定1920×1080舞台、中心锚点缩放、明亮校园配色、彩色模块化卡片和CSS几何装饰技术实现。

## 2026-07-28 答辩PPT稳重活力风格预览生成

实现了第三组三套更稳重但保留学生产品活力的答辩PPT HTML封面预览，并将项目展示名称统一为“校园π”，通过蓝绿主色、暖白背景、模块化信息面板和固定舞台中心缩放技术实现。

## 2026-07-28 校园π答辩演示稿生成

实现了校园π实训项目16页HTML答辩演示稿，通过选定沉稳产品汇报风格、固定1920×1080舞台、键盘/触控翻页、内容分层卡片、流程图和架构图CSS布局技术实现。

## 2026-07-28 答辩PPT文字遮挡修复

实现了答辩PPT第3页团队分工表格和第16页亮点卡片与副标题的间距优化，通过为对应页面增加专用布局类并下移内容区的CSS定位技术实现。

遇到了副标题换行后覆盖下方表格和卡片的问题，用页面级样式覆盖方法解决。

## 2026-07-28 拉取最新仓库并保留本机接口地址

实现了GitHub远端main分支最新前端与后端代码同步，通过git fetch和git pull --ff-only快进合并技术实现。

遇到了远端更新会把本机ApiService接口地址改为其他局域网地址的问题，用保留新增接口代码并仅恢复baseUrl配置的方法解决。

## 2026-07-28 Web端投诉审核流程优化

实现了Web管理端投诉记录从直接处理改为弹窗审核流程，通过前端Naive UI弹窗、后端新增投诉聊天记录查询接口、退回投诉接口和投诉状态扩展技术实现。

遇到了原投诉处理接口缺少审核上下文且没有退回路径的问题，用按投诉单查询双方聊天记录并区分处理投诉/退回投诉两个操作的方法解决。

## 2026-07-28 投诉审核聊天记录匹配修复

实现了投诉审核弹窗聊天记录查询范围优化，通过后端按投诉人与被投诉人双向匹配聊天会话、优先匹配同商品会话并兜底查询双方最近会话的技术实现。

遇到了历史聊天会话用户顺序或商品关联不完全一致导致弹窗显示暂无聊天记录的问题，用双向用户匹配和最近会话兜底方法解决。

## 2026-07-29 个人资料与拼车体验修复

实现了编辑资料页用户名和手机号一次保存到数据库，通过后端新增 update-profile 合并接口、前端 ApiService 增加 updateProfile 方法、保存按钮统一调用 saveProfile 并同步 AppStore 用户信息技术实现。

实现了拼车发布者发布后自动加入、取消或退出后首页我的约车不再展示、我的发布页面可自由滑动，通过发布事务内插入乘客记录、我的约车过滤已取消和未加入记录、移除各 Tab 列表内容固定高度技术实现。

遇到了我的发布列表内容容器固定 height('100%') 导致 Scroll 只能拖动一点就回弹的问题，用让列表内容按真实高度撑开的方法解决。

## 2026-07-29 拼车取消通知与投诉通道复用

实现了拼车删除必须先取消、取消后自动通知拼车者、拼车详情联系发起者和拼车投诉入口，通过复用已有聊天会话创建/发送消息接口、已有投诉提交接口、后端取消拼车事务内发送取消通知并清理乘客关系技术实现。

实现了拼车投诉原因自动带上下文，通过拼车详情和聊天页识别拼车取消消息后向投诉页传入 reasonPrefix，投诉仍进入原投诉处理通道但原因中标明拼车场景技术实现。

遇到了多个页面都能触发拼车删除导致可能绕过“先取消再删除”的问题，用后端 delete 强校验状态、约车列表和我的发布页前端同步拦截的方法解决。

## 2026-07-29 拼车取消通知实时推送修复

实现了拼车取消通知实时送达，通过取消拼车保存聊天消息后复用 WebSocket pushChatMessage 推送 message 和 unread_update 技术实现。
遇到了取消拼车只写入聊天记录但没有走聊天 WebSocket 推送，导致消息 Tab 不实时出现的问题，用发布者取消和发布者退出统一复用取消拼车通知逻辑的方法解决。

## 2026-07-29 拼车列表加入状态刷新修复

实现了拼车列表加入状态实时刷新，通过在列表 ForEach key 中加入 joined、status 和 currentPassengers 状态字段，触发卡片状态变化后重新渲染技术实现。
遇到了拼车详情页已更新但列表卡片仍显示加入拼车的问题，用避免同一订单 id 卡片复用旧渲染状态的方法解决。

## 2026-07-29 拼车取消固定通知内容修复

实现了发布者取消拼车后自动向拼车者发送“取消此次约车活动”消息，通过取消拼车前读取乘客列表、复用聊天会话创建和消息发送通道、再清理乘客关系技术实现。
遇到了取消拼车通知内容不符合要求且已取消订单可能直接返回不补发消息的问题，用固定取消通知文案并在清理乘客关系前统一发送消息的方法解决。

## 2026-07-29 拼车删除入口移除

实现了移动端拼车删除入口移除，通过去掉拼车详情页、拼车列表卡片和我的发布拼车卡片中的删除按钮及对应前端删除弹窗逻辑技术实现。
