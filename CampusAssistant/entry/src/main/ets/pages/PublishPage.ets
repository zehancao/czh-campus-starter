@Entry
@Component
struct PublishPage {
  build() {
    Column() {
      Text('何隆 - 发布商品 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
