@Entry
@Component
struct ConversationListPage {
  build() {
    Column() {
      Text('何隆-会话列表 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
