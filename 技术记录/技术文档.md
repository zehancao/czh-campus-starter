# 技术文档

- 实现了商品图片数据库批量更新与部署，通过执行 product_images_update.sql 将 190 个商品的 images 字段更新为 gen_prod_{id}.png 路径，并将图片从项目目录拷贝到服务器 upload-dir（/Users/caozehan/Campus-Server/uploads）实现。遇到了项目目录与服务器 upload-dir 路径不一致导致图片 404 的问题，用 cp 批量拷贝到服务器实际上传目录的方法解决。
- 实现了商品占位图批量生成（190 张零 credits），通过 Python + Pillow 脚本从 campusdb2.sql 解析商品数据，按 19 个分类匹配 Sunshine Campus 配色渐变背景（教材暖橙、电子海洋蓝、生活阳光黄、运动绿、个护粉、其他灰），生成对角渐变 + 分类标签 + 大字图标 + 白色信息面板（商品名 + 成色标签 + 价格含原价划线）的品牌占位图，存到 Campus-Server/uploads/ 并输出 SQL UPDATE 语句文件实现。遇到了 Pillow 不支持 macOS 彩色 emoji 字体渲染导致 emoji 显示异常的问题，用分类关键词汉字替代方案解决。
- 实现了 MainPage 首页布局创新重构（四大模块），通过 Hero Banner 暖橙渐变顶栏（学校名+学期周徽章+时段问候+头像）替换原灰白问候行、Quick Entry 9个入口包裹在白色浮动卡片中负margin叠在Banner底部形成视觉层次、今日课表+我的活动合并为 TimelineView 时间线视图（新增 TimelineItem 接口和 getCombinedTimeline 排序方法，课程橙色标识线+活动海洋蓝标识线）、校园公告从纵列改为水平Scroll横向滚动卡片（240px宽，分类标签颜色映射：教务蓝/紧急红/活动橙）实现。新增 getGreeting/getTodayDate/getAnnouncementCategoryColor/getAnnouncementCategoryBg 四个辅助方法。
- 实现了 Sunshine Campus 阳光校园设计系统的圆角、按钮、阴影规范统一落地到代码，通过修正 Theme.ets 中 5 个圆角常量（sm/md/lg/xl/pill）及语义别名、新增按钮尺寸/渐变/阴影常量（buttonHeightSm 32 / Md 44 / Lg 52、buttonRadius=radiusPill 胶囊形、btnGradientSunrise/Ocean 渐变数组）、ShadowStyle 轻量化对齐 rgba 阴影实现。6 个页面硬编码 borderRadius 替换为 Theme 常量引用，MainPage 按规则未动。
- 将13个页面的自定义导航栏统一替换为 NavHeader 组件，通过 NavHeader 的 title 属性和 @BuilderParam rightSlot 实现标题与右侧按钮的复用。对 ChatPage 的在线状态和商品号信息，移至 NavHeader 下方独立 Row 展示。
- 优化了二手交易列表页和商品详情页的视觉一致性，通过将搜索框、分类标签、商品卡片、详情信息区和底部联系按钮统一为 Theme 主色、白色圆角卡片、柔和阴影和浅灰背景实现。遇到了交易模块橙色主按钮、灰色缺图占位和列表底部被 Tab 遮挡导致风格割裂的问题，用主色替换、浅色占位和底部安全间距的方法解决。
- 优化了编辑资料页头像上传弹窗，通过将系统 ActionSheet 替换为页面内自定义底部 Sheet，并精简为"修改头像、从相册选择、取消"三段式结构实现。遇到了原弹窗悬浮在页面中部且与保存按钮视觉重叠的问题，用 Stack 全屏遮罩加底部对齐弹窗的方法解决。
- 优化了编辑资料页用户名输入行的视觉表现，通过去除 TextInput 灰色背景和圆角胶囊，仅保留透明输入区域与右对齐文字实现。
- 实现了个人课表、手动添课和教室课表页面的整体视觉统一优化，通过复用 Theme 设计令牌、浅灰页面背景、白色圆角浮层、柔和阴影、胶囊按钮、紧凑表单输入框和课程卡片阴影实现。
- 优化了空教室查询页的信息密度和工具页观感，通过压缩筛选卡片高度、收紧查询面板间距、降低结果摘要字号层级、缩短教室结果卡片高度，同时保留新拟态白卡、圆角和阴影风格实现。遇到了页面过度卡片化导致首屏结果露出不足的问题，用保留视觉统一但压缩纵向空间的方法解决。
- 实现了空教室查询页的新拟态样式优化，通过将筛选条件区改为大圆角白色浮层卡片、将查询按钮改为胶囊主按钮、将结果摘要和教室列表卡片统一使用 Theme 设计令牌、柔和阴影和圆角标签实现。
- 修复了编辑资料页和修改密码页返回键点击无反应的问题，通过将公共 NavHeader 返回图标封装为 44x44 点击热区，并改用当前页面上下文 Router 执行 back 返回实现。遇到了返回事件绑定在小图标上导致实机点击区域不稳定的问题，用扩大点击容器和统一公共返回组件的方法解决。
- 实现了个人中心和编辑资料相关页面的新拟态样式统一，通过在个人中心信息卡、资料编辑卡、密码修改表单中复用 Theme 设计令牌、圆角卡片、柔和阴影、胶囊按钮和浅灰输入框实现。
- 实现了课表日期星期对齐和空教室默认按当前周查询，通过将学期开始日期归一到所在周周一，并让课表页与空教室页复用统一周次计算工具实现。遇到了课表页和空教室页分别计算周次导致查询条件不一致的问题，用共享日期工具统一周次和日期计算的方法解决。
- 实现了空教室查询与课表教室占用匹配，通过后端同时比较教室号和教学楼加教室号的归一化名称实现。遇到了课表中教室可能保存为"教学楼+教室号"、教室表只保存教室号导致占用教室仍被返回的问题，用统一去空格并双格式匹配的方法解决。
- 实现了课表页日期兜底显示和切回本周按钮尺寸位置调整，通过在学期开始日期未加载时按今日日期和当前周反推周日期，并将按钮改为固定宽度右边界定位实现。遇到了接口数据未到达时日期退化为序号的问题，用本地日期兜底计算的方法解决。
- 实现了鸿蒙前端美化交互原型页面，通过单文件 HTML+CSS+JS 构建手机壳预览，含 5 个底部 Tab（首页/二手/社团/消息/我的）和 8 个核心子页面（商品详情/聊天/课表/空教室/失物招领/设置），所有组件统一使用 Theme.ets 设计令牌（颜色/圆角/阴影/间距/字号），支持 Tab 切换、页面导航栈、发送消息、收藏切换、筛选过滤等交互。遇到原型需在浏览器预览的问题，用 python3 http.server 本地服务的方法解决。
- 增强了 Theme.ets 设计令牌，新增字号阶梯（fsDisplay/fsTitle/fsSubtitle/fsBody/fsCaption/fsSmall）和圆角语义别名（cardRadius/heroRadius/chipRadius），为后续全站页面统一美化奠定基础。
- 修复了DevEco Studio 26提示"工程构建配置需要升级"的问题，通过在hvigor/hvigor-config.json5中添加@ohos/hvigor-ohos-plugin和@ohos/hvigor依赖（版本6.26.1），并在用户目录创建.npmrc配置HarmonyOS npm镜像仓库实现。遇到了hvigor插件未安装和.npmrc缺失导致依赖下载失败的问题，用补全hvigor-config.json5依赖声明和配置repo.harmonyos.com镜像地址的方法解决。
- 实现了首页快捷入口卡片色调统一和今日课表/我的报名等高对齐，通过将空教室、社团互动、AI助手三张快捷卡的颜色从各自语义色（success/secondary/danger）统一改为 Theme.primary 主色+primaryLight 浅底，并为两个并列卡片的父级 Column 添加 .height('100%') 强制拉伸到 Row 高度实现底边对齐。遇到了 constraintSize({ minHeight: 200 }) 只能设下限无法真正等高的 ArkUI Row 子元素不对齐问题，用 .height('100%') 拉伸的方法解决。遇到了 .height('100%') 导致 Row 高度撑满 Scroll 造成校园公告上方大片空隙的问题，改用 constraintSize({ minHeight: 200 }) 自然高度方案解决。遇到了 hero 紫色卡片和校园公告卡片 .width('100%') + .margin({ left: 18, right: 18 }) 总宽度超出屏幕的问题，用去掉 .width('100%') 保留 margin 自动约束的方法解决。
- 实现了首页视觉统一优化：1）今日课表和我的报名标题并入卡片内，与校园公告格式一致，通过将 backgroundColor/borderRadius/shadow 从内层内容 Column 提升到外层 Column 实现；2）失物招领大卡从橙黄渐变改为 primaryLight 浅靛蓝底色，角标同步从橙色改为靛蓝色系；3）课表/空教室/社团互动/AI助手四个快捷入口卡片从靛蓝浅底改为白色背景+阴影，与界面整体白底风格统一。
- 实现了首页图标和标题字体的视觉优化：1）将课表/空教室/社团互动/AI助手四个快捷入口的 SVG 图标分别替换为 emoji（📅/🏫/🎭/🤖）字号 26，提升图标辨识度；2）将今日课表和我的报名两个卡片的标题字号从 13 调整为 16，与校园公告标题字号统一。
- 修正了今日课表"完整课表"跳转链接的箭头字符不统一问题，通过将 "完整课表 →" 改为 "完整课表 >"，与我的报名/校园公告的 "更多 >" 箭头字符统一。
- 实现了首页玻璃拟态（Glassmorphism）视觉效果，通过将首页 Scroll 区域底层添加紫蓝粉渐变背景（#667eea → #A855F7 → #F472B6），并将所有白色卡片（快捷入口×4、失物招领、今日课表、我的报名、校园公告）的背景改为半透明 rgba(255,255,255,0.65) + backdropBlur(16) 毛玻璃模糊 + 半透明白色细边框 rgba(255,255,255,0.4) 实现。保留了 hero 渐变大卡的原色作为视觉焦点，其他卡片透出底层渐变色彩形成透明玻璃感。
- 实现了首页从玻璃拟态全面切换为新拟态（Neumorphism）设计风格，通过以下改动实现：1）Theme.ets 设计令牌全面替换——背景色 bgPage/bgCard 改为 #E0E5EC 同色无边界设计、文字色 textPrimary 改为 #4A5568 柔和深灰、圆角全面增大（radiusMd:20/radiusLg:24）、新增 nmRaised/nmPressed 右下暗阴影（#A3B1C6）模拟浮雕凸起；2）MainPage.ets 首页移除紫蓝粉渐变背景和所有 backdropBlur 毛玻璃效果，Hero 大卡去掉渐变改用纯色 bgCard + nmRaised 阴影、4 个快捷入口卡片从玻璃透明改为新拟态凸起、失物招领/今日课表/我的报名/校园公告 7 个卡片统一改为 bgCard 同色背景 + nmRaised 大阴影浮起效果；3）Hero 卡内统计数字和文字从白色改为 Theme.textPrimary/textTertiary 柔和深色体系。通过将卡片背景色与页面背景色统一、仅通过右下暗阴影制造浮起感的 neumorphism 技法实现。
- 强化了新拟态浮雕效果以接近 AI 概念图标准，通过以下改动实现：1）Theme.ets 阴影系统全面增强——nmRaised 阴影 radius 20→30、offset 5→8、color 透明度 0x80→0xA0（更暗更明显），新增 nmRaisedLg 阴影（radius:40/offset:12/color:#A3B1C6B0）专用于 Hero 大卡；2）圆角全面加大——radiusSm 10→14、radiusMd 20→24、radiusLg 24→32、radiusPill 26→30 让卡片更圆润；3）新增新拟态渐变专用色 nmSurface(#EAEEF3 高光色) 和 nmShade(#C8CFDA 阴影色)；4）MainPage.ets 首页 8 个关键卡片（Hero、4 快捷入口、失物招领、今日课表、我的报名、校园公告）将 backgroundColor 替换为 inline 写死的 linearGradient（角度 135，颜色 [['#EAEEF3', 0.0], ['#E0E5EC', 0.5], ['#C8CFDA', 1.0]]），从左上高光到右下阴影模拟真实光源，解决鸿蒙 ArkUI 单层阴影无法实现双阴影浮雕的痛点。遇到老板反馈"实现效果与 AI 概念图差距大"的问题，用"linearGradient 模拟光源 + 增强右下阴影 + 加大圆角"组合方案让浮起感更接近设计稿。\n- 二次加强新拟态浮雕立体感以解决"背景偏暗、卡片浮起感弱"问题，通过以下改动实现：1）Theme.ets 背景色从 #E0E5EC 改为 #ECF0F5（更白更亮，让阴影更突出），新增 nmShadow(#8B9BB4 更深暗影) 和 nmHighlight(#FFFFFF 高光) 专用色，渐变色 nmSurface 从 #EAEEF3 改为 #FAFCFE（更白高光），nmShade 从 #C8CFDA 改为 #B8C1D0（更深阴影）；2）nmRaised 阴影重调：radius 30→20、offset 8→10、color 透明度 0xA0→0xC0 且颜色改为 #8B9BB4（更深更聚焦的右下投影，浮起感更强），nmRaisedLg 同步加强（offset 12→14、color #8B9BB4D0），sm/md/lg 普通阴影也用更深的 #8B9BB4 色；3）MainPage.ets 首页 8 个卡片渐变全部从 ['#EAEEF3', 0.0], ['#E0E5EC', 0.5], ['#C8CFDA', 1.0] 替换为更白更深的 ['#FAFCFE', 0.0], ['#ECF0F5', 0.5], ['#B8C1D0', 1.0]，让卡片内对比度更强、浮雕感更明显。遇到老板反馈"背景还是不够白、立体感小"的问题，用"页面背景变白 + 卡片渐变对比拉大 + 阴影颜色变深"组合方案让浮起效果接近设计稿。\n- 重构新拟态阴影策略以解决"阴影在卡片内部看起来很脏"问题，通过以下改动实现：1）MainPage.ets 首页 8 个卡片的 linearGradient 内部渐变全部去掉，改为纯 backgroundColor(Theme.bgCard)，让卡片本体干净同色，不再有内部脏色；2）nmRaised 外部阴影大幅增强——radius 20→30、offset 10→12、color #8B9BB4C0→#8B9BB4D0，nmRaisedLg 同步拉满（radius 30→40、offset 14→16、color #8B9BB4D0→#8B9BB4E0），浮雕凸起完全靠外部投影实现。通过"卡片本体纯色统一 + 外部投影拉满"新拟态正确技法实现。
- 将新拟态配色系统切换为纯白色调，通过以下改动实现：1）Theme.ets 中 bgPage 从 #ECF0F5 改为 #FFFFFF、bgCard 改为 #FFFFFF、bgInput 改为 #F4F5F7、bgHover 改为 #F0F1F3，实现纯白背景 + 纯白卡片的极致干净画面；2）纯白卡片在纯白背景上仅通过外部阴影（nmRaised/nmRaisedLg 的 #8B9BB4 投影）来制造分层感和立体感，凸起效果在白色底上更加明显。通过"纯白统一底色 + 强外部投影"技法实现。\n- 通过 Stack 双层嵌套技术实现新拟态卡片的真实 3px 厚度感，解决"卡片很薄、立体感不足"问题：1）Theme.ets 新增 nmThickness（#EBEDF1 厚度底层色，比背景深一档），nmRaised 阴影调整为 radius:18 offset:{4,6} color:#8B9BB4D0（与底层厚度对齐的精准投影），nmRaisedLg 调整为 radius:28 offset:{8,10} color:#8B9BB4E0；2）MainPage.ets 首页 4 个快捷入口全部改造为 Stack 双层结构 —— 外层 Stack 包裹两层 Column，底层 Column 用 Theme.nmThickness 偏深灰色 + 圆角 + offset({x:3, y:3}) 向右下偏移模拟"厚度边"，上层 Column 用 Theme.bgCard 纯白 + 1px Theme.nmThickness 浅色边框 + 圆角作为卡片本体，整体 Stack 加 shadow(ShadowStyle.nmRaised) 制造浮起感。通过"Stack 双层错位 + 浅色描边 + 精准外部投影"组合方案实现老板参考图中的"卡片有厚度"效果（卡片上半边高光、右下半边双线、阴影自然）。
- 重构新拟态层次感方案，解决"纯白底+纯白卡片效果差、实机渲染差距太大"的问题，通过以下改动实现：1）Theme.ets 背景色 bgPage 从纯白 #FFFFFF 回调到柔和灰白 #F0F3F6，利用色差自然衬托白色卡片——这是新拟态的核心技法（背景略深于卡片，层次自然出现）；2）移除 nmThickness、nmSurface、nmShade 等不再使用的辅助色，简化设计令牌；3）阴影系统全面重调——所有阴影 offsetX 设为 0（正投下阴影更干净），nmRaised radius:18→12 offsetY:6→4 color 透明度 D0→90（更轻盈自然），nmRaisedLg radius:28→20 offsetY:10→6（大卡片阴影收敛），避免之前重阴影 + 纯白背景导致的"脏感"；4）MainPage.ets 首页 4 个快捷入口从 Stack 双层结构回退为简洁单层 Column（白色背景 + 圆角 + 轻阴影），解决之前偏移错位在实机上看起来像渲染 bug 而非立体厚度的问题；5）今日课表和我的报名列表项补充 shadow(ShadowStyle.sm)，保证灰白背景上每条内容都有轻微浮起。通过"背景微灰衬托白卡 + 轻阴影 + 单层卡片"组合方案实现干净自然的新拟态效果。
- 重构底部 Tab 栏布局并将课表独立为子页面，通过以下改动实现：1）Tab 栏从「首页/课表/交易/AI/我的」改为「首页/交易/消息/AI/我的」——Tab 1 从 ScheduleContent 改为 MarketContent、Tab 2 从 MarketContent 改为新增 MessageContent（轻量占位 Column）并注册 ic_message/ic_message_active 新图标（聊天气泡 SVG 风格与现有图标统一，非激活#9CA3AF/激活#4F46E5）；2）消息 Tab 点击时在 onChange 中通过 router.pushUrl 跳转到 ConversationListPage，不实际占据 tab 展示区域，实现"点击即跳转"的轻 Tab 体验；3）首页"我的课表"快捷入口从 this.currentTab=1（切到课表 Tab）改为 router.pushUrl({ url:'pages/SchedulePage' }) 独立页面跳转，课表不再作为 Tab 嵌入 MainPage，而是在已有 SchedulePage.ets 中独立渲染（含学期加载、周选择器、7x10 课表网格、课程详情弹窗等完整功能）；4）保留了 MainPage 内 ScheduleContent/ScheduleHeader/ScheduleGrid 等 Builder（避免连锁删除其他页面的依赖），仅在 Tab 级别解除挂载。通过在 Tab onChange 中检测 index===2 触发页面跳转的技法实现消息入口。
- 将首页 4 个快捷入口按钮改造为新拟态双层按钮风格（参考图效果），通过以下改动实现：1）创建 QuickEntry @Builder 复用方法，接收 emoji/label/targetPage 三个参数；2）每个按钮使用 Stack 双层结构——底层 Column 用 #C8CFDA 深灰色 + borderRadius(28) + offset({x:4,y:4}) 向右下偏移模拟按钮"厚度边"，上层 Column 用 linearGradient（angle:180，#FFFFFF→#F4F5F7）模拟顶部柔和高光效果，整体 Stack 设置 clip(false) 允许偏移超出；3）图标从 SVG Image 改为大号 Emoji 文字（📅📅/🏫/🎉/🔍），fontSize 32，视觉更醒目；4）按钮尺寸从 64×72 增大到 68×88（Stack 容器 72×92），圆角从 24 增至 28，外部阴影用柔和投影（radius:14, offsetY:5, #A3B1C670）。通过"底部厚度暗色层 + 顶部渐变高光 + 柔和弥散投影"组合方案实现新拟态凸起按钮效果。遇到了两个问题：1）Stack 外层无 borderRadius 导致阴影按直角矩形扩散，看起来像方框，用给 Stack 添加 borderRadius(28) 的方法解决；2）8 位 hex 阴影色（#A3B1C670 及 ShadowStyle.md 的 #A3B1C680）在 ArkUI 下均被解析为 #AARRGGBB 格式，蓝色通道只剩 70-80 导致泛黄，最终改用 6 位 hex 纯中性灰 #ABB3C0（不包含 alpha 通道，无格式歧义，RGB 三通道均衡无色彩偏向），radius 增至 16 补偿透明度损失。3）顶层渐变方向调试：根据 ArkUI 官方文档，GradientDirection.RightBottom = 左上→右下（#0% 在左上），对应 angle: 45°。`angle: 135` 改回 `direction: GradientDirection.RightBottom` 枚举写法，更明确（避免 angle 数字歧义）。新拟态立体感主要由"投影位置 + 厚度层偏移 + 圆角层叠"三要素构成，渐变高光是辅助。最初用 #FFFFFF → #E4E8EE 对比度太小（差异仅 17）看不出高光，加深到 #FFFFFF → #D0D5DD（差异 47）后左上半边的高光感明显增强，与右下半边厚度+阴影形成完整的"光从左上斜照"浮雕效果。4）解决"右上左下白边凸出"问题：根因是底层 Column（68×88, offset(4,4)）只覆盖 Stack 的中间区域，Stack 圆角"右上"和"左下"两个角落（约 4×4 px）没有 Column 覆盖，透出页面背景 #F0F3F6（与上层 Column 圆角边缘 #FFFFFF 纯白存在明显对比）形成"白边凸出"。修复方案迭代：v1 在 Stack 外层包 Column + backgroundColor('#F0F3F6')，仍有白边（页面背景与上层圆角边缘颜色不一致）；v2 直接给 Stack 自身设 linearGradient，direction: GradientDirection.RightBottom, colors: [['#FFFFFF', 0.0], ['#D0D5DD', 1.0]]（与上层 Column 渐变方向和颜色完全一致）。这样圆角空白区域显示 Stack 自身的渐变色，与上层 Column 圆角边缘的渐变色精确对齐（相同位置相同颜色），实现无缝融合，不再有"白边凸出"。同时简化代码，去掉外层 Column 包装。5）回滚纯白方案，恢复新拟态立体效果：底层厚度层 Column 用 #C8CFDA，上层 Column 和 Stack 均用 GradientDirection.RightBottom 对角渐变（#FFFFFF → #D0D5DD），配合右下阴影（shadow radius:16, color:#ABB3C0, offsetY:4），保持"光从左上斜照"的浮雕立体感。6）上层内容 Column 改为纯白色 backgroundColor('#FFFFFF')：去除渐变，与页面底色 #F0F3F6 形成明确区分，底部厚度层 #C8CFDA + Stack 渐变保留，立体感和层次感不变。7）登录页和注册页按钮统一改为新拟态风格：将 LoginPage 和 RegisterPage 中原有的 Button（蓝色背景、白色文字）替换为 Stack 双层结构——底层 Column 填满 Stack 区域（width/height 100%）用 #C8CFDA 做厚度，上层 Column 用 #FFFFFF 纯白 + margin({right:4, bottom:4}) 露出右下厚度边，Stack 外层 linearGradient（#FFFFFF → #D0D5DD）+ borderRadius(28) + shadow 与首页快捷入口按钮风格完全一致。登录按钮宽度 85%，注册按钮宽度 100%，高度统一 52（48+4 厚度边），fontColor 从白色改为 Theme.textPrimary 深色文字。通过 margin 方式（而非 offset）实现全宽自适应按钮的厚度露出效果。8）输入框和选择器统一改为新拟态风格：LoginPage 的学号/密码输入框、RegisterPage 的学号/姓名/密码/确认密码/手机号输入框以及 SelectField 选择器组件（年级/学院/专业/班级/籍贯）全部用相同 Stack 双层结构包裹——底层 Column #C8CFDA 厚度 + 上层 TextInput/Row #FFFFFF 纯白 + margin({right:4, bottom:4}) 露出右下厚度边，外层 linearGradient + borderRadius(28) + shadow 与按钮风格统一。去除原有 Theme.bgInput 背景色、borderRadius(8)、shadow(ShadowStyle.sm)，SelectField 的 margin({top:12}) 从内层 Row 移到外层 Stack 保持间距一致。9）首页顶部问候区拆分为两个独立新拟态组件：将原先一整块新拟态卡片拆分为——左侧文字卡片（"你好，XX" + "college · major"）用 auto 宽度（去掉 layoutWeight(1)）+ height(73) 的新拟态 Stack 卡片（#C8CFDA 厚度 + #FFFFFF 纯白上层 + linearGradient + shadow）；右侧头像从卡片内提取为独立 56×56 新拟态圆形 Stack（背景纯白 #FFFFFF 做"白色 cushion/ring"、底层 #C8CFDA 40×40 圆 offset(3,3) + 上层 40×40 头像/首字母）。两者用 Row + VerticalAlign.Center 水平排列，Blank() 弹性填充中间间距。文字卡片 padding({left:28, right:24, top:16, bottom:14}) 大幅增加左侧留白（之前是 20）。圆形头像 shadow radius 大幅减小至 8（之前 12），offsetY 从 4 减到 3，整体阴影范围更紧凑，避免视觉上"拉出去太大"。文字卡片 shadow radius 同步从 16 减到 10，offsetY 从 4 减到 3，统一收敛阴影强度。
- 修复了首页问候卡片新拟态效果丢失问题，通过给外层文字卡片 Stack 加 .layoutWeight(1) 显式占满 Row 剩余空间，shadow 从手写 {radius:10, color:'#ABB3C0'} 替换为 Theme.ShadowStyle.nmRaised（radius:12, color:'#A3B1C690', offsetY:4），头像 Stack shadow 同步升级。遇到了 Stack 没显式 width 但内部 Column 写 width('100%').height('100%') 导致父子循环依赖、Stack 算不出尺寸、厚度层和阴影和 linearGradient 全部渲染异常的问题，用 .layoutWeight(1) 让父容器获得确定尺寸再用 100% 拉伸子元素的方法解决。
- 修复了问候区三处细节问题：1）问候文字卡片和下方课表快捷入口竖直不对齐——原因是 QuickEntry Row 用了 SpaceAround 在第一个元素前有额外间距，改成 SpaceBetween 让第一个元素左对齐；2）文字卡片右下阴影太粗——厚度层 margin 从 4px 缩到 2px；3）头像阴影看起来加在头像上而非白色底上——原因是内部厚度层 offset(3,3) 超出白色 Stack 边界，加 .clip(true) 裁切让 shadow 紧贴白色底。
- 简化问候区新拟态结构：去掉问候卡片和头像的 #C8CFDA 灰色厚度层（Stack 双层结构），改为纯白色图层 + ShadowStyle.nmRaised 阴影，shadow 原点紧贴白色底；问候卡片 Column 加 margin({left:8}) 微调与下方课表按钮对齐；Row 从 margin 改为 padding 统一左右间距；QuickEntry Row 恢复 SpaceAround 保持与左边留出距离。
- 给问候卡片和头像增加立体感：用极淡表面渐变（linearGradient #FFFFFF→#F2F4F8，左上→右下模拟光源）替代纯白背景，配合加强阴影（卡片 radius:14/offsetY:6，头像 radius:12/offsetY:5），通过"表面光源渐变 + 右下方浮起投影"两层叠加实现无厚度层的新拟态立体效果。
- 问候卡片和头像立体感照搬 QuickEntry 双层 Stack 结构：底层 #C8CFDA 厚度层 offset(4,4) 露出右下厚度边，上层 #FFFFFF 纯白内容，外层 Stack linearGradient(#FFFFFF→#D0D5DD) + clip(false) + shadow(radius:16/#ABB3C0/offsetY:4)，参数完全一致。
- 修复问候卡片和头像重叠问题：去掉 Stack 的 layoutWeight(1)，让卡片按内容宽度自适应不占满 Row；厚度层改为 Stack 自身 backgroundColor('#C8CFDA') + 内部白色 Column margin({right:4, bottom:4}) 露出右下灰色底实现（替代 offset Column）。修复头像阴影位置问题：去掉内部 offset 厚度 Column，Stack 自身白色底 + linearGradient + shadow，shadow 原点在白色外层。
- 头像左移 + 增强立体感：头像 Stack 加 margin({right:8}) 向左收，内部补回 offset Column(#C8CFDA, offset(4,4)) 厚度层（与 QuickEntry 结构一致），shadow 原点仍在白色外层 Stack 上。
- 头像修复：去掉 Stack 内部 offset(4,4) 灰色厚度 Column，shadow 现在明确加在白色外圆边缘上；margin right 从 8 调至 12 让头像更左移；问候卡片未动，左侧仍与课表对齐。
- 今日课程和我的活动各加白色卡片容器：每个 Column 加 backgroundColor('#FFFFFF') + borderRadius(16)，标题 padding 加 top:16 让文字不贴顶，两卡片间用 Blank().width(12) 留间距。
- 今日课程和我的活动从左右并排改为上下放置：外层 Row 改 Column，卡片 layoutWeight(1) 改 width('100%')，Blank width 12 改 height 12。
- 课程卡片和活动卡片缩小框框：padding 从 {left:12, right:12, top:6, bottom:6} 收至 {left:10, right:10, top:4, bottom:4}，margin bottom 从 6 缩至 4。
- 取消今日课程和我的活动大卡片白色背景（去 backgroundColor('#FFFFFF') 和 borderRadius(16)），每个小 item 用 Stack 双层结构增加立体感：底层 #C8CFDA Column offset(3,3) 露出厚度，上层 #FFFFFF Row 纯白内容，外层 Stack linearGradient(#FFFFFF→#D0D5DD) + clip(false) + shadow(radius:12/#ABB3C0/offsetY:3)，与问候区/快捷入口结构一致。
- 修复课程/活动 item 灰色厚度层撑满整 Stack 的 bug：之前用 inner Column width('100%').height('100%') offset(2,2) 方式，动态高度场景下 height('100%') 会撑爆。改为外层 Stack 自己设 backgroundColor('#C8CFDA') 当厚度色，内层白色 Row 用 margin({right:2, bottom:2}) 暴露右下 2px 灰色边（与问候卡片同款结构）。遇到了动态高度 Stack 内部 child 不能用 height('100%') 否则会撑满父容器可用空间的问题，用外层背景色+内层 margin 的方式解决。
- 统一立体元素圆角：课程和活动 item 的 borderRadius 从 8 调到 16（内外两层 Stack 都改），与问候卡片/快捷入口的 28 风格一致，更圆润。
- 从远程仓库拉取并合并最新代码（origin/main 由 76f838d 更新至 a8ac8c1，涉及 NavHeader、AddCoursePage、EditProfilePage、EmptyRoomPage、MainPage、PasswordChangePage、ProductDetailPage、RoomSchedulePage、SchedulePage 等 10 个文件），通过 git stash 暂存本地未提交的 MainPage UI 改动、git merge --ff-only 拉取远程、再 git stash pop 自动合并实现。遇到了远程新提交也改动了 MainPage.ets 与本地未提交 UI 改动同文件可能冲突的问题，用先备份本地文件再 stash 暂存、拉取后 stash pop 自动合并（无冲突标记）的方法解决。合并后保留了本地更优的 AI 聊天 baseUrl 写法（基于 ApiService.baseUrl 动态推导 8000 端口），替代远程硬编码的 192.168.1.3:8000。
- 修复了失物招领列表不显示图片的问题，通过给 LFListCard 增加 getFirstImageUrl 方法解析 images 字段（兼容 JSON 数组与逗号分隔两种格式）并在卡片左侧优先用 Image(`${ApiService.baseUrl}${url}`) 渲染真实图片、无图时回退 emoji 占位实现。遇到了 images 字段是字符串而非数组、且可能是 `["a","b"]` 或 `a,b` 两种格式的问题，用去除括号/引号并按逗号取首元素的方法解决。
- 实现了社团活动封面图全链路（发布+列表+详情），通过后端为 ClubActivity 实体与 ClubActivityVO 增加 coverImage 字段、ClubActivityService.toVO 透传封面、ClubActivityController 新增 `/api/club-activity/upload-cover` 上传接口（复用文件写入逻辑），以及前端在 ClubActivityPublishPage 增加封面选择与上传（调用 ApiService.uploadClubCover）、ClubActivityListPage 卡片顶部与 ClubActivityDetailPage 顶部展示封面图（ApiService.baseUrl 拼接）实现。遇到了 club_activities 表无封面列导致发布报 SQL 错误的问题，用给数据库执行 ALTER TABLE 增加 cover_image 列并同步更新 campusdb2.sql DDL 的方法解决。
- 实现了头像支持相册上传与 emoji 自选，通过后端新增 `POST /api/user/update-avatar` 文本接口（UserService.updateAvatar 兼容任意字符串）、前端新增 ApiService.updateAvatar 方法，并将头像渲染统一抽象为 common/AvatarView 组件（以 `/` 开头视为图片、否则视为 emoji、为空显示昵称首字母），在 MainPage 和 EditProfilePage 复用，同时在 EditProfilePage 头像弹窗增加 emoji 表情网格选择实现。遇到了 emoji 头像无法存入 MySQL 的问题，用连接前执行 `SET NAMES utf8mb4` 确保 4 字节字符正确写入（users.avatar 列为 utf8mb4）的方法解决。
- 插入了多模块测试数据，通过把 generated-designs 目录 7 张 UI 稿复制到后端绝对上传目录 `/Users/caozehan/Campus-Server/uploads/` 并重命名为 gen_1~gen_7.png，向 products（images 存 JSON 数组）、lost_founds（images 存 JSON 数组）、club_activities（cover_image）写入带图数据，并为 5 个用户设置 emoji 头像实现。
- 批量插入测试数据实现全覆盖，通过编写 SQL 脚本向 campusdb2 数据库插入 27 名用户（覆盖全部 34 个省/直辖市/自治区、全部 11 个学院）和 181 条商品（每个商品分类精确 10 条，共 19 分类×10=190 条）实现。遇到了 users.password 必须用 BCrypt 哈希而非明文、products.images 和 products.tags 字段为 JSON 类型不接受空字符串的问题，用 Java BCrypt.hashpw 生成哈希、images 设 NULL、tags 用 JSON_ARRAY() 函数的方法解决。
- 重新导出完整的 campusdb2 全库 dump 并推送到 GitHub 供组员使用，通过 mysqldump（本地 8.0.46 客户端与服务器版本一致，--single-transaction --routines --events --databases campusdb2 对库只读导出）覆盖掉旧坏文件实现。遇到了原 campusdb2.sql 损坏（缺失 personal_timetable 表、INSERT 列数不匹配无法导入）的问题，用重新导出得到 34 张完整表、再导入临时库验证可正常导入、最后 git commit + push origin/main 的方法解决，真实库未被改动。
- 实现了 web-admin 的酷炫数据大屏 UI 版本（深色科技风、霓虹光效、数字滚动 KPI、动态中国地图与饼图/柱状图动效），通过新建独立的 src/views/dashboard/Screen.vue（沿用原 echarts 与 /api/admin/stats 接口、china.json 地图）并新增路由 /dashboard-screen 与侧边菜单「酷炫大屏」入口实现，未改动原 dashboard/Index.vue。
- 将酷炫数据大屏从管理员后台抽离为独立公开展示页，通过把路由 /screen 提为顶层路由（脱离 Layout 侧边栏）、在路由守卫 publicPaths 中放行 /screen、并改用原生 fetch（避开 request.ts 的 401 自动跳转登录页拦截器）实现；大屏顶栏新增「管理后台登录」入口跳 /login，原管理员登录流程与 dashboard/Index.vue 均未改动。
- 修复了公开大屏 /screen 打开后所有业务数据为空的问题，通过后端 JwtAuthFilter 的免鉴权白名单新增 `/api/admin/stats/` 与 `/api/admin/announcement/list` 两个路径实现；此前大屏为免登录公开页未带管理员 token，被过滤器拦截返回 401，导致 KPI、地图、图表与公告全为空，放开后公开页即可正常展示聚合统计数据，且不影响其他 admin 写接口的权限安全。
- 调整了管理后台退出登录后的跳转目标，通过将 Layout.vue 的 handleLogout 在清除 token 后由 router.push('/login') 改为 router.push('/screen') 实现；管理员点击「退出」后不再停留于登录页，而是进入免登录的公开数据大屏，避免一直显示管理员登录态。
- 放大了酷炫大屏中中国地图的展示区域并增强配色，通过将 main-grid 中间列宽由 1.8fr 调为 2.4fr、panel-map 的 flex 由 2.6 调为 3.6、地图 zoom 由 1.1 调为 1.15、layoutSize 由 92% 调为 94% 实现；地图视觉映射色阶改为对比更强的深蓝→蓝→青→绿→黄→橙→红八段热力色，并将省份边界颜色提亮为 rgba(160,220,255,0.65)，使数值分布更醒目。
- 调整了大屏布局与榜单数量，通过将热门分类 TOP 的 slice(0,6) 改为 slice(0,5) 实现只展示前五；并把「近 7 日公告发布量」折线图从中间栏底部的 panel-mini 移出，作为第 5 个小方框（chart-box）与注册用户/商品总数等四个 KPI 卡片并排置于顶部 KPI 行（kpi-row 列数改为 repeat(5,1fr)），折线图改为隐藏坐标轴的迷你 sparkline 样式（.chart-box / .cb-title / .cb-chart 样式，复用 kpi-card 的霓虹边框与四角装饰），地图因此独占中栏获得更大空间。
- 实现了大屏 KPI「公告数量」改为「活跃用户」，通过后端 users 表新增 last_login_time 字段、UserService.login 登录时写入最后登录时间、StatsController.getOverview 统计近 7 日登录用户数返回 activeUserCount，前端 Screen.vue 将第 3 个 KPI 改为绿底「活跃用户」并绑定 activeUserCount 实现。遇到了 selectCount 返回 Long 无法赋给 Integer 的编译错误，用将 activeUserCount 类型改为 Long 的方法解决。
- 修复了 Bug #1 浏览历史点击商品跳转错误，通过把 MyHistoryPage.ets 的 goToDetail() 跳转目标由 pages/MainPage 改为 pages/ProductDetailPage（传 productId，ProductDetailPage.resolveId() 兼容读取），实现了从浏览历史直接进入商品详情。并把「近 7 日公告发布量」折线图从中间栏底部的 panel-mini 移出，作为第 5 个小方框（chart-box）与注册用户/商品总数等四个 KPI 卡片并排置于顶部 KPI 行（kpi-row 列数改为 repeat(5,1fr)），折线图改为隐藏坐标轴的迷你 sparkline 样式（.chart-box / .cb-title / .cb-chart 样式，复用 kpi-card 的霓虹边框与四角装饰），地图因此独占中栏获得更大空间。
- 修复了登录后跳转MainPage闪退的问题，通过完全移除router.getParams()调用、改用AppStore.pendingTab全局变量传递tab参数实现。遇到了router.getParams()在aboutToAppear中无参数时返回undefined/null，ArkTS严格模式下as强转触发不可捕获TypeError: Cannot load property of null or undefined导致进程被杀的问题，用AppStore全局状态替代router params传参的方法解决。
- 修复了收藏页点击商品跳转错误，通过把 MyFavoritesPage.ets 的 goToDetail() 跳转目标由 pages/MainPage 改为 pages/ProductDetailPage（传 productId，ProductDetailPage.resolveId() 兼容读取），实现了从收藏列表直接进入商品详情（与 Bug #1 同源问题一并修复）。
- 修复了 Bug #3 网络错误无用户提示，通过在 ApiService.ets 的 request 方法 catch 块中引入 @kit.ArkUI 的 promptAction 直接 showToast 提示“网络错误，请检查后端是否启动”，让所有经 request 的网络异常统一弹窗提示用户，无需逐页面改动。
- 修复了 Bug #10 三个上传接口无文件类型校验，通过在 UserController.uploadAvatar、ProductController.uploadImage、ClubActivityController.uploadCover 三个方法中增加扩展名白名单 Set.of("jpg","jpeg","png","gif","webp")，取原文件名扩展名（去点小写）做校验，非白名单类型返回“不支持的文件类型”，避免上传 .jsp/.exe 等危险文件。
- 修复了 Bug #15 反馈内容 XSS 漏洞，在 FeedbackService.sendFeedback 中对 userName/contact/content 经新增的私有 escapeHtml 方法做 HTML 转义（& < > " ' 转实体），再拼入邮件 HTML 模板，阻断反馈内容注入脚本在收件邮件客户端执行。
- 商品模块（清单第一节）批量修复：① #13 ProductVO 增加 categoryName 字段，ProductService.toVO 按 categoryId 查 product_categories 回填分类名；② #16 商品浏览量改用 LambdaUpdateWrapper.setSql("view_count = view_count + 1") 数据库原子自增，消除“读-改-写”竞态；③ #23 searchProducts 新增 ProductMapper.selectSearchWithSeller 用 SQL LIKE 在数据库层过滤标题/描述且 status=1，去掉全表拉取后内存过滤；④ #18 ApiService.parseFavoriteList 改用 JSON.parse 解析 productImages 并加 try-catch 容错；⑤ #22 抽公共 uploadFile(endpoint,uri) 方法，uploadImage/uploadAvatar/uploadClubCover 均委托调用，消除重复代码（后端 mvn -o compile 通过，鸿蒙端无 lint 错误）。
- 修复了 Bug #12 每次查询删除过期活动，删除 ClubActivityService 中 getList/getMyActivities 对 cleanExpiredActivities() 的调用并移除该方法；getList 改为加 activity_time >= now() 条件在数据库层过滤过期活动（过期活动保留不物理删除，仅列表不展示），避免查询即误删活动与报名记录。
- 修复了 Bug #24 ClubActivityService N+1 查询，新增 toVOList() 批量方法：先收集全部活动/用户 id，用 userMapper.selectBatchIds 和 activityRegistrationMapper.selectList(w.in(activityId, ids)) 各查一次，在内存按 id 组装 VO；getList/getMyActivities 直接调用 toVOList，单条 toVO 委托 toVOList(单元素列表)，getMyActivities 的活动查询也改为 selectBatchIds 批量。SQL 由 O(N×M) 降至约 3 次，消除高数据量下响应慢与数据库连接打满风险（mvn -o compile 通过）。
- 修复了 Bug #11 投诉提交即扣信用分（恶意投诉可攻击他人、管理员审核形同虚设），通过把 ComplaintService.submitComplaint 改为只插入 status=0 的待审核记录、不再立即扣分，并加防重复校验（同一人对同一被投诉人同一商品已有待审核投诉则抛异常报错），同时把 ComplaintService.processComplaint（管理端“处理”按钮 /api/complaint/process/{id}）改为 status 0→1 的同时真正执行被投诉人信用分 -5 并保持幂等（status≠0 不重复处理），让“管理员审核通过”才真正生效、驳回/重复处理不会误扣分；前端聊天页 ChatPage 信息栏新增「投诉」入口（otherUserId>0 且非本人时显示），点击跳 MyComplaintsPage 并带入 defendantId/defendantName/productId（>0 才传）复用现有投诉弹窗，MyComplaintsPage 提交提示改为“投诉已提交，等待管理员审核”、顶部提示条改为“投诉经管理员审核通过后，被投诉人信用分 -5”。
- 修复了 Bug #21 课表页“切回本周”按钮位置硬编码，通过去掉 Button 的 `position({ x: 292, y: 610 })` 绝对定位，把按钮包进一个全尺寸浮层 `Row`（`.width('100%').height('100%').justifyContent(FlexAlign.End).alignItems(VerticalAlign.Bottom)`）底部右对齐，并加 `.hitTestBehavior(HitTestMode.None)` 让点击穿透到课表，按钮固定在课表区右下角、不受屏幕宽度影响，且不干扰课表网格显示与滚动。补修该按钮文字截断：原 `.width(88)` 在 ArkUI 中 width 含 padding（内容区仅 40px），“切回本周”四字被裁成“切”，已去掉固定宽度改为按内容自适应，四字完整显示。
- 回退了 Bug #28 SchedulePage 课程卡片高度的尝试性修改，确认原 `.height(courseDuration * SCHEDULE_SECTION_HEIGHT - 8)` 才是连堂大课跨节显示的正确做法：显式像素高度强制卡片跨节（溢出父 ScheduleCell 高 66）实现两节课连上一节大课的视觉。constraintSize({ minHeight, maxHeight }) 方案受父容器约束限制会被夹成 66，导致大课不再跨节，因此已恢复原 `.height()` 方案并标记 #28 为「已验证无需修改」。
- 修复了 Bug #4 修改密码页用 changePassword(old, old) 验证旧密码的副作用，通过去掉 `PasswordChangePage.verifyOldPassword()` 中调用 `ApiService.changePassword(old, old)` 的逻辑（该调用会把密码改成自身、存在副作用），改为仅做非空校验后进入第二步，真正的原密码校验在 `submitNewPassword` 调用 `changePassword(old, new)` 时由后端一次性完成（吻合清单方案“不在提交前验证，仅确认修改时一次调用”）。
- 修复了 Bug #5 AppStore 属性非响应式，经核实现有页面（MainPage 的 aboutToAppear+onPageShow、EditProfilePage 等）已在页面生命周期把 AppStore.userInfo 同步到本地 @State，登录态/用户信息变更后会在页面重新展示时刷新，已规避症状；同时把 AppStore 的 setToken/setUserInfo/logout 同步写入 AppStorage（key: appToken/appIsLoggedIn/appUserInfo），提供可观察通道，需实时响应登录态的页面可用 @StorageLink 读取，真正支持响应式（采用 AppStorage 桥接而非 @ObservedV2，零编译风险、向后兼容）。
- 修复了 Bug #7 AdminController 返回含 password 的 User 实体（敏感信息泄露），通过新增 AdminUserVO（不含 password 字段，含其余用户字段），AdminService.getAllUsers 查询后逐字段转换为 AdminUserVO 列表，AdminController 的 /api/admin/user/list 返回 `Result<List<AdminUserVO>>`，管理端用户列表不再暴露 BCrypt 密码哈希。
- 实现了桌面小组件（今日课程卡片）功能，通过创建 form_config.json 卡片配置（2×4动态卡片，定时刷新）、EntryFormAbility.ets 卡片生命周期（onAddForm/onUpdateForm从Preferences读取课表数据通过FormBindingData传递）、WidgetCard.ets 卡片UI（日期周次+今日课程列表+下节课提示+无课休息态+点击跳转SchedulePage）、WidgetDataHelper.ets 数据缓存工具（主应用写入Preferences/卡片Ability读取）、module.json5注册FormExtensionAbility、string.json添加卡片名称描述资源实现。遇到了preferences.getSync方法不存在的问题，用preferences.getPreferencesSync替代的方法解决。
- 实现了5个新功能入口添加到首页，通过创建StudyTogetherPage/CarpoolPage/ConfessionWallPage/CampusSafetyPage/StudySharePage 5个空页面（NavHeader+emoji图标+功能描述+"开发中"提示），并在main_pages.json注册、MainPage首页快捷入口区新增第二行5个按钮（🤝约人/🚗拼车/💌表白墙/🆘安全/📚资料）实现。新拟态QuickEntry组件与原有4个入口风格一致，为组员后续开发预留页面骨架。
- 修复了添加课程页TextPicker选择后数值固定不到输入框的bug，通过将TextPicker的selected参数从单向绑定改为双向绑定（`selected: $$this.tempDayIndex`），并将自定义Stack遮罩弹窗替换为原生`bindSheet`半模态弹窗实现。遇到了TextPicker的selected参数仅在初始化时生效、后续@State变化不会驱动Picker滚动位置更新的问题，用`$$`双向绑定语法解决；遇到了自定义Stack弹窗position+translate定位导致点击事件被遮罩层拦截、弹窗内控件不可交互的问题，用bindSheet原生半模态弹窗替代解决；遇到了bindSheet的SheetOptions不支持dragDown属性导致编译报错的问题，用onDisappear回调替代解决；遇到了同一组件不能绑定两个bindSheet的限制，将bindSheet分别绑定到两个独立的Row行组件上解决。
- 实现了添加课程页教室地点从文本输入改为二级联动选择器，通过后端新增`/api/classroom/rooms`接口（按building查询该楼所有教室号）、前端ApiService新增getRoomsByBuilding方法、AddCoursePage将教室地点TextInput替换为bindSheet弹窗内双列TextPicker（左列选教学楼→右列选教室号，选教学楼时联动加载教室列表，确认后拼接building+roomNo写入classroom字段）实现。确保手动添课的教室名称与空教室查询的教室命名格式一致，解决之前手动输入教室名格式不统一导致空教室查询匹配不上的问题。
- 修复了添加课程后课表不显示的问题，通过在SchedulePage增加onPageShow生命周期重新加载课表实现。遇到了router.back()返回后aboutToAppear不重新触发导致课表数据不刷新的问题，用onPageShow每次页面显示时调用loadSchedule的方法解决。同时补充了后端ScheduleService中班级课和个人课VO的weekParity字段设置（根据remark判断单周/双周/全选）。
- 统一了教室命名格式为4位数字编码（楼号+层号+教室号，如1310=1号楼3层10号），通过将class_timetable表的classroom字段从"1教101"格式批量更新为"1101"纯数字格式、前端添加课程页教室选择器确认后只取roomNo（4位数字）而非拼接building+roomNo、classrooms表保持building="1教"roomNo="1101"不变实现。确保class_timetable.classroom与classrooms.room_no格式一致，后端ClassroomService的isSameRoom匹配逻辑（occupiedKey.equals(roomNoKey)）可正确比对，空教室查询不再因格式不一致而匹配失败。重新导出campusdb2.sql并删除了旧格式个人课表数据。
- 修复了课表显示概率性问题（有时显示有时不显示），通过在SchedulePage增加isLoadingSchedule防重入标志、loadSchedule入口检查isLoadingSchedule避免并发执行实现。遇到了aboutToAppear调loadSemester（内含loadSchedule）和onPageShow调loadSchedule并发竞争、两个异步请求同时修改scheduleList导致数据覆盖的问题，用isLoadingSchedule布尔锁确保同一时刻只有一个loadSchedule在执行的方法解决。
- 修复了课表不显示周六日个人课程的根本原因——添加课程时未传semesterId导致课程存入错误的学期，通过在SchedulePage跳转AddCoursePage的router.pushUrl参数中增加semesterId字段、AddCoursePage用该字段提交到后端实现。遇到了personal_timetable的semester_id=1但当前学期semester_id=2导致getMySchedule查不到个人课的问题，用SchedulePage传参加semesterId+数据库UPDATE修正已有数据的方法解决。
- 修复了桌面卡片不显示今日课程的问题，通过在WidgetDataHelper.saveTodayCourses保存Preferences后主动调用formProvider.updateForm通知卡片刷新、在EntryFormAbility.onAddForm保存formId到Preferences、onRemoveForm删除formId实现。遇到了主应用写入Preferences后卡片不自动刷新的问题，用saveFormId/removeFormId管理卡片ID+notifyWidgetUpdate主动推送数据的方法解决。WidgetDataHelper参数类型从UIAbilityContext改为Context以兼容FormExtensionContext。
- 实现了教室地点支持选择+手动输入两种方式，通过将AddCoursePage教室区域拆分为两行（"选择教室"行打开bindSheet选择器+"手动填写"行TextInput输入操场/体育馆等非标准地点）、手动输入和选择器互相同步到classroom字段实现。删除了废弃的computeClassroomLabel方法。
- 修复了空教室查询未纳入个人课表占用的问题，通过在ClassroomService.getEmptyRooms中新增PersonalTimetableMapper查询personal_timetable表（按dayOfWeek/section/week/semesterId条件）、将查出的classroom合并到occupiedRooms列表中实现。确保手动添加的课程所在教室在空教室查询中被正确排除（如周六上午1-2节概率论占用1间教室，空教室数从500变499）。
- 实现了全局主题从暗色"极光"靛蓝系切换为阳光暖橙系，通过将Theme.ets中9个色值区块（主色/次色/语义色/文字/背景/边框/新拟态/课表配色/阴影）全部替换为Sunshine Campus暖色Token（主色#FF8C42暖橙、背景#FFFBF5奶油白、次色#06AED5海洋蓝、阴影改为暖色系），保持变量名完全不变、所有页面自动继承新配色实现。配套输出完整设计系统规范文档generated-designs/sunshine-campus-design-system.html（含CSS变量、组件样式、新旧迁移对照）。
- 实现了首页布局二次重构（三大区域），通过将MainPage HomeContent中"我的活动"从TimelineView中拆出改为横向滚动卡片（Scroll+Row，200px宽卡片含coverImage封面图/emoji占位+标题+场地+人数）、"校园公告"从横向滚动卡片改为纵向排列列表（Row布局：分类标签+标题/摘要+日期）、今日课表保留在今日日程TimelineView中实现。
- 实现了桌面卡片点击跳转SchedulePage，通过在EntryAbility中新增onNewWant处理卡片router事件（parseWidgetRouter解析want.parameters.params中targetPage）、onCreate中同样解析首次冷启动的卡片参数、loadTargetPage根据selectPage决定加载目标页或默认LoginPage实现。卡片WidgetCard已配置postCardAction发送router事件（targetPage='pages/SchedulePage'）。
- 清理了StudentTimetable.java残留代码，通过删除无任何引用的孤立实体类（无Mapper/Service/Controller使用，对应student_timetable表已废弃被personal_timetable替代）实现。
- 修复了"今日日程"标题和内容不匹配的问题，通过将标题改为"今日课程"并从getCombinedTimeline中移除活动数据（活动已在独立"我的活动"横向卡片区域展示，不应混入课程时间线）实现。
- 修复了首页"我的活动"卡片显示emoji占位而非封面图的问题，通过在loadMyActivities解析时补充coverImage/currentParticipants/maxParticipants字段读取实现（之前只解析了id/club/title/venue/activityTime，coverImage为空字符串导致fallback到emoji）。
- 优化了首页配色分区：我的活动区域增加海洋蓝元素（封面图上叠加半透明蓝底社团标签、场地文字改蓝色、更多>改蓝色、卡片加浅蓝边框）、校园公告区域统一为暖橙色元素（分类标签统一橙色字体+浅橙底、日期改橙色、更多>改橙色、去掉分类颜色映射逻辑）实现。
- 优化了今日课程卡片配色，通过用Theme.scheduleCardBg/scheduleCardFg（6色轮换：暖橙/海洋蓝/琥珀黄/红色/粉色/紫色）替换纯白背景，时间线标识线和时间文字同步匹配对应前景色实现。多门课程不再同色，每门课按索引取不同配色。
- 编写了二期5个功能的开发文档（空课约人/拼车回家/表白墙/校园安全/学习资料），通过在/Users/caozehan/Desktop/czh-campus-starter/二期功能文档/目录下创建6份文档（00-通用规范+5份功能文档）实现。每份包含建表SQL、后端接口设计、前端Model/ApiService/页面文件命名、页面注册、业务逻辑说明、参考代码。
- 取消了"空课约人"功能，通过删除StudyTogetherPage.ets、main_pages.json注册、首页QuickEntry入口（第二行5个入口改为4个：拼车/表白墙/安全/资料）实现。理由：约人功能本质是发布-加入模式，与活动报名重复，缺乏真正空课查人能力。
- 实现了表白墙/树洞完整功能（前后端），通过后端建3表（confession_posts/confession_comments/confession_likes）+10个Java文件（3Entity+3Mapper+2VO+1Service+1Controller）+9个API接口（列表/详情/发布/删除/点赞/评论/举报/我的帖子/上传图片）+前端3页面（ConfessionWallPage分类Tab列表+ConfessionDetailPage详情评论+ConfessionPublishPage发布页）+Model追加ConfessionPost/ConfessionComment+ApiService追加10个方法实现。支持匿名/实名、分类筛选（表白粉色/树洞蓝色/吐槽橙色/交友紫色）、点赞切换、评论、图片上传。
- 修复了表白墙闪退问题，原因是后端VO的images字段是List<String>类型，JSON序列化后是数组而非字符串，前端Model声明为string类型导致调用.trim()时报TypeError("trim is not callable")。通过将ConfessionPost.images改为string[]类型、解析时遍历Object[]转string数组、getFirstImage直接取数组第一个元素解决。
- 修复了表白墙评论失败的问题，原因是后端comment接口用@RequestParam接收content和isAnonymous（从URL query取参），但前端把这两个参数放在Body里传，后端接收不到。通过将前端ApiService.commentConfession改为URL query传参（encodeURIComponent编码content）解决。
- 统一了首页9个快捷入口emoji背景色为暖橙浅色（Theme.primaryLight #FFF4E6），通过移除QuickEntry的colorType参数、固定backgroundColor为primaryLight实现（之前0/1双色交替不够统一）。
- 实现了表白墙后端全链路（10个文件），通过创建3个Entity（ConfessionPost/ConfessionComment/ConfessionLike）、3个Mapper、2个VO（ConfessionPostVO含images JSON→List解析和匿名用户遮蔽/ConfessionCommentVO）、ConfessionWallService（getList分页+分类筛选、getDetail含评论和点赞状态、publish/delete/like切换/comment/report/getMyPosts共7个方法，LambdaQueryWrapper+LambdaUpdateWrapper原子自增）、ConfessionWallController（9个接口含upload文件上传复用UUID+白名单校验模式）实现。
- 实现了学习资料共享后端全链路（9个文件），通过创建3个Entity（StudyShare/StudyShareLike/StudyShareFavorite）、3个Mapper、1个VO（StudyShareVO含uploaderName/uploaderAvatar/liked/favorited）、StudyShareService（getList分页+课程名/分类筛选、getDetail含点赞/收藏状态、publish/delete/like切换/favorite切换/download原子自增/report/getMyShares/getMyFavorites共9个方法，LambdaQueryWrapper+LambdaUpdateWrapper.setSql原子自增）、StudyShareController（12个接口含upload支持pdf/doc/ppt/xls/图片/压缩包+uploadCover图片专用）实现。
- 实现了学习资料共享前端全链路，通过StudySharePage列表页（分类Tab横向滚动+课程名搜索+ShareCard卡片含文件类型图标/封面图/大小/下载次数）、StudyShareDetailPage详情页（资料信息+封面/文件类型预览+点赞/收藏/下载三栏操作+删除/举报）、StudySharePublishPage上传页（分类选择+标题+课程名+描述+DocumentViewPicker选文件上传+封面图上传）、Models.ets追加StudyShareItem、ApiService追加12个方法（含uploadStudyShareFile/uploadStudyShareCover）实现。
- 修复了非图片文件上传失败的问题，原因是uploadFile方法硬编码了.jpg后缀和contentType='image/jpeg'，上传PDF/DOC等文件时MIME类型不对。通过新增getExtFromUri（从URI提取扩展名）和getMimeType（扩展名→MIME映射，覆盖pdf/doc/docx/ppt/pptx/xls/xlsx/图片/zip/rar）方法，动态设置文件名后缀和contentType解决。
- 实现了PDF预览和文件下载功能，通过StudySharePreviewPage（@kit.PDFKit的PdfView组件加载PDF，PdfController.loadDocument+registerPageCountChangedListener/registerPageChangedListener，上下翻页+页码显示）、详情页新增预览按钮（PDF用应用内PdfView，doc/ppt/xls/图片用系统filePreview.openPreview）、下载按钮改为真正下载文件到沙箱filesDir（http GET请求+expectDataType:ARRAY_BUFFER+fileIo.writeSync保存）实现。遇到了PDF解析失败(PARSE_ERROR_FORMAT)的问题，原因是http请求默认返回string类型导致写入乱码，用expectDataType:http.HttpDataType.ARRAY_BUFFER解决。
- 优化了资料详情页和上传页体验：有封面图时右下角显示文件类型角标（半透明黑底+emoji+格式名）、上传页添加格式提示"支持 PDF / Word / PPT / Excel / 图片 / 压缩包"。
- 统一了所有列表页分页加载机制，通过每页20条(PAGE_SIZE)+currentPage/hasMore/isLoadingMore状态管理+refreshList(重置page=1替换数据)+loadMore(追加下一页)+List.onReachEnd触底加载+底部"加载更多/没有更多了"提示实现。改造了5个列表页：ConfessionWallPage、StudySharePage、LostFoundListPage、ClubActivityListPage、MainPage商品列表。同时ApiService.getClubActivityList/getMyClubActivities追加page/size参数。
- 实现了学习资料列表分页加载功能，通过新增currentPage/hasMore/isLoadingMore状态变量和PAGE_SIZE常量，将loadList重构为refreshList（重置页码+替换数据）+loadMore（追加数据+触底判断），Scroll+Column改为List+ListItem结构并绑定onReachEnd触发加载更多，列表底部显示LoadingProgress"加载更多..."或"- 没有更多了 -"状态提示实现。
- 实现了失物招领列表分页加载功能，通过新增currentPage/hasMore/isLoadingMore状态变量和PAGE_SIZE常量，将loadList重构为refreshList（重置页码+替换数据）+loadMore（追加数据+触底判断），Scroll+Column改为List+ListItem结构并绑定onReachEnd触发加载更多，列表底部显示LoadingProgress"加载更多..."或"- 没有更多了 -"状态提示实现。
- 实现了社团活动列表页分页加载功能，通过ApiService.getClubActivityList和getMyClubActivities新增page/size参数（URL追加page=${page}&size=${size}），ClubActivityListPage新增currentPage/hasMore/isLoadingMore状态变量和PAGE_SIZE=20常量，将loadActivities重构为refreshActivities（重置页码+替换数据+判断hasMore）+loadMore（追加数据+触底判断），所有筛选/切换回调改为调refreshActivities，List绑定onReachEnd触发loadMore，列表底部显示LoadingProgress"加载更多..."或"- 没有更多了 -"状态提示，保留Refresh下拉刷新调doRefresh实现。同步修复MainPage.loadMyActivities调用getMyClubActivities(1,5)适配新签名。
- 实现了拼车回家全栈功能，通过后端2表(carpool_orders/carpool_passengers)+2Entity+2Mapper+2VO(CarpoolOrderVO/PassengerVO)+CarpoolService(7方法:getList/getDetail/publish/join/cancelJoin/cancelOrder/getMyOrders)+CarpoolController(7接口)、前端3页面(CarpoolPage列表+目的地搜索/状态筛选/我的拼车切换+分页加载、CarpoolDetailPage详情+乘客列表+加入/退出/取消按钮、CarpoolPublishPage发布+DatePicker+TextPicker时分选择)+Model追加CarpoolOrder/PassengerItem+ApiService追加9方法(含parseCarpoolOrders/parsePassengers)+main_pages.json注册2新页面实现。
- 实现了校园安全一键报全栈功能，通过后端2表(safety_reports/safety_contacts)+2Entity+2Mapper+2VO(SafetyReportVO/SafetyContactVO)+CampusSafetyService(6方法:submitReport/getList/getDetail/handleReport/getMyReports/getContacts)+CampusSafetyController(7接口含upload图片上传)、前端3页面(CampusSafetyPage主页含紧急联系电话横向卡片+状态筛选Tab+我的求助切换+分页加载、SafetyReportDetailPage详情+处理信息+处理操作面板、SafetyReportPage一键求助+紧急类型选择+位置描述+图片上传)+Model追加SafetyReport/SafetyContact+ApiService追加8方法(含parseSafetyReports/parseSafetyContacts)+main_pages.json注册2新页面实现。
- 修复了CampusSafetyPage定位权限3个ArkTS编译错误，通过将abilityAccessCtrl.AccessManager类型标注去掉（改用let推断）、将locationTimeout/locationPriority改为locatingPriority/locatingTimeoutMs（匹配新版SingleLocationRequest接口）、将LocatingPriority.PRIORITY_LOCATING_SPEED替代废弃的LocationRequestPriority.FIRST_FIX实现。遇到了ArkTS严格模式禁止any/unknown、namespace无AccessManager导出、SingleLocationRequest字段名变更的问题，用去掉显式类型标注+更新接口字段名的方法解决。
- 实现了web-admin管理后台求助记录查看与处理页面，通过新建safety/Index.vue（NDataTable列表+状态筛选+类型映射+处理弹窗+调用/api/safety/list和/api/safety/handle接口）、注册路由/safety、Layout.vue侧边栏增加ShieldOutline图标菜单项+pageTitle映射实现。
- 修复了求助记录在admin不显示的两个问题，通过将前端CampusSafetyPage长按求助type从中文"紧急求助"改为英文"emergency"（与web-admin typeMap的key一致）、后端CampusSafetyService.getList增加内存分页（subList截取page/size）解决。遇到了type中英文不匹配导致admin类型列显示空白、后端getList未做分页导致前端传page/size无效的问题，用统一英文key+手动分页的方法解决。
- 实现了求助定位经纬度逆地理编码为具体地址，通过geoLocationManager.isGeocoderAvailable检查服务可用性+geoLocationManager.getAddressesFromLocation(ReverseGeoCodeRequest)逆地理编码，从GeoAddress中提取administrativeArea+locality+subLocality+roadName+placeName拼接为完整街道地址（如"北京市海淀区中关村南大街"），逆地理编码失败时回退显示经纬度坐标实现。
- 实现了定位失败时手动输入位置的兜底方案，通过检测isLocationEnabled和定位异常，定位失败时显示手动输入框（placeholder"例如：3号教学楼2层"）+"提交求助"按钮，用户输入位置后调用doSubmit提交实现。遇到了模拟器默认关闭系统位置服务(The location switch is off, 错误码3301100)导致getCurrentLocation必定失败的问题，用定位失败→手动输入→再提交的降级方案解决。
- 实现了二手交易商品列表分页加载，通过后端ProductController.getList追加page/size参数+ProductService.getProductList内存分页(subList截取)、前端ApiService.getProductList追加page/size参数、MainPage新增productPage/productHasMore/productLoadingMore状态+refreshProducts(重置page=1)+loadMoreProducts(追加下一页)+parseProduct抽取公共解析方法+MarketContent从Scroll+ForEach改为List+ListItem+onReachEnd触底加载+底部"加载更多/没有更多了"提示实现。
- 重构了表白墙页面为朋友圈风格，通过去掉ConfessionDetailPage详情页跳转、卡片内直接展示头像(AvatarView)+昵称+相对时间(刚刚/X分钟前/X小时前/X天前)+分类标签、内容全展开不截断、多图九宫格(Flex+FlexWrap单图200px宽其余3列等分)、图片点击全屏预览(Swiper+黑底+页码指示+关闭按钮)、评论点击底部弹出评论列表+输入框+发送按钮、点赞评论直接在卡片内操作实现。
- 修复了拼车发布失败(后端返回一大段错误信息)的问题，通过后端CarpoolOrder实体departureTime字段添加@JsonFormat(pattern="yyyy-MM-dd HH:mm")注解使Jackson能将前端传的"yyyy-MM-dd HH:mm"字符串反序列化为LocalDateTime、前端CarpoolPublishPage的TextPicker onChange回调去掉`as string`断言改用typeof窄化实现。遇到了Jackson默认无法将字符串反序列化为LocalDateTime导致HTTP 400返回大段JSON序列化错误的问题，用@JsonFormat注解指定日期格式的方法解决。
- 排查联系我们发送反馈"网络错误"问题，通过在ApiService.request的catch中添加具体错误信息输出(String(err))替代笼统的"网络错误，请检查后端是否启动"，方便定位前端HTTP请求失败的具体原因实现。后端feedback/submit接口curl测试正常返回200，问题疑在前端侧。
- 修复了联系我们发送反馈"网络错误"(catch输出"object")的问题，通过ApiService.request的HttpRequestOptions添加expectDataType:HttpDataType.STRING强制返回字符串、response.result用typeof窄化替代`as string`断言实现。遇到了未指定expectDataType时response.result返回object类型导致`as string`断言抛异常被catch捕获的问题，用显式声明返回类型+安全类型检查的方法解决。
- 删除了个人中心"AI助手""消息通知""修改密码"三个菜单项，通过MainPage个人中心区域移除三行Row+onClick、EditProfilePage移除"修改密码"行实现。
- 重构了"我的发布"页面为5分类Tab（商品/活动/拼车/表白墙/资料），通过MyPublishPage改用Tabs+5个TabContent+5种卡片组件(MyProductCard/MyActivityCard/MyCarpoolCard/MyConfessionCard/MyStudyShareCard)、懒加载(onTabChange检查loaded标记未加载则请求API)、ApiService新增parseClubActivityList/parseConfessionPostList/parseStudyShareList三个公共parse方法提取自各页面私有方法实现。商品Tab保留上架/下架/删除操作，其他4类展示列表+点击跳转对应详情页。
- 修复了"我的发布"3个问题：1) 切Tab闪回商品（全局isLoading隐藏了Tabs→改为每个Tab独立loading状态）；2) 活动/资料详情页参数错误（传activityId/shareId→改为id匹配详情页取参）；3) 表白墙去掉跳转+加图片九宫格展示（MyConfessionCard去掉onClick/onTap、内容全展开、Flex+FlexWrap九宫格单图200px其余100px）。
- 修复了拼车详情页参数传不过去的问题，通过将params从`{ 'orderId': item.id } as Record<string, Object>`改为`{ 'id': \`${item.id}\` } as Record<string, string>`，匹配CarpoolDetailPage的取参方式(params['id']为string→parseInt)实现。
- 修复了"我的发布"商品图片不显示的问题，通过ApiService.parseProductList中images字段解析增加双重处理（typeof object→as string[]；typeof string→手动去除[]和引号再split），与ProductDetailPage的images解析逻辑一致实现。遇到了后端Product实体images字段为String类型（JSON字符串如'["/uploads/xxx.png"]'），Jackson序列化后不是数组而是字符串，前端直接`as string[]`失败的问题，用typeof判断+字符串解析的方法解决。
- 修复了桌面卡片今日课程4节课时第四节显示不全的问题，通过减小WidgetCard各元素间距（日期行margin 8→4、课程行margin 4→2、课程指示条height 28→24、课程名字体13→12/教室10→11、下节课padding/margin缩减、整体padding缩减）使4节课程在2x4卡片高度内完整展示实现。
- 我的收藏页面增加了资料收藏展示，通过MyFavoritesPage并行请求getMyFavorites+getMyStudyShareFavorites、新增StudyShareFavoriteCard组件（封面图/emoji+标题+课程名·分类+点赞下载数+资料标签）、商品和资料混排展示（ForEach key用product_/study_前缀区分）、总数统计合并两种收藏数实现。
- 首页新增"我的约车"板块，通过MainPage新增@State myCarpools+loadMyCarpools(调ApiService.getMyCarpoolOrders+parseCarpoolOrders)、在"我的活动"和"校园公告"之间展示约车行程卡片（出发地→目的地+出发时间+人数/容量+状态标签：招募中绿/已满黄/已取消灰）、点击跳转CarpoolDetailPage实现。
- 首页板块标题对齐与跳转优化：我的约车标题padding与我的活动/校园公告统一(left:16,right:16)对齐；我的活动"更多"点击跳ClubActivityListPage；我的约车"更多"点击跳CarpoolPage；校园公告去掉"更多"按钮。
- 校园活动页面添加返回键，通过替换原Row标题为NavHeader组件（含返回箭头+标题+右侧组局按钮）实现。
- 表白墙添加删除功能，通过ApiService新增deleteConfession方法(POST /api/confession-wall/delete?postId=)、ConfessionWallPage的PostCard顶部右上角对作者显示"删除"按钮(判断AppStore.getInstance().userInfo.id===item.userId)、MyPublishPage的MyConfessionCard加onDelete回调实现。
- 全模块删除功能统一完善：1)后端新增ClubActivityController.delete和CarpoolController.delete接口(验证userId===作者才允许删除，级联删除报名/乘客记录)；2)ApiService新增deleteClubActivity/deleteCarpool方法；3)各列表页(活动/拼车/资料)卡片右上角对作者显示"删除"按钮；4)各详情页(活动/拼车)底部加"删除"按钮(资料详情已有)；5)MyPublishPage五个Tab全部加删除按钮+确认弹窗(AlertDialog.show)；6)删除按钮均远离点赞/评论操作区，统一使用确认弹窗防误删。
