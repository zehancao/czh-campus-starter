# 技术文档

- 优化了二手交易列表页和商品详情页的视觉一致性，通过将搜索框、分类标签、商品卡片、详情信息区和底部联系按钮统一为 Theme 主色、白色圆角卡片、柔和阴影和浅灰背景实现。遇到了交易模块橙色主按钮、灰色缺图占位和列表底部被 Tab 遮挡导致风格割裂的问题，用主色替换、浅色占位和底部安全间距的方法解决。
- 优化了编辑资料页头像上传弹窗，通过将系统 ActionSheet 替换为页面内自定义底部 Sheet，并精简为“修改头像、从相册选择、取消”三段式结构实现。遇到了原弹窗悬浮在页面中部且与保存按钮视觉重叠的问题，用 Stack 全屏遮罩加底部对齐弹窗的方法解决。
- 优化了编辑资料页用户名输入行的视觉表现，通过去除 TextInput 灰色背景和圆角胶囊，仅保留透明输入区域与右对齐文字实现。
- 实现了个人课表、手动添课和教室课表页面的整体视觉统一优化，通过复用 Theme 设计令牌、浅灰页面背景、白色圆角浮层、柔和阴影、胶囊按钮、紧凑表单输入框和课程卡片阴影实现。
- 优化了空教室查询页的信息密度和工具页观感，通过压缩筛选卡片高度、收紧查询面板间距、降低结果摘要字号层级、缩短教室结果卡片高度，同时保留新拟态白卡、圆角和阴影风格实现。遇到了页面过度卡片化导致首屏结果露出不足的问题，用保留视觉统一但压缩纵向空间的方法解决。
- 实现了空教室查询页的新拟态样式优化，通过将筛选条件区改为大圆角白色浮层卡片、将查询按钮改为胶囊主按钮、将结果摘要和教室列表卡片统一使用 Theme 设计令牌、柔和阴影和圆角标签实现。
- 修复了编辑资料页和修改密码页返回键点击无反应的问题，通过将公共 NavHeader 返回图标封装为 44x44 点击热区，并改用当前页面上下文 Router 执行 back 返回实现。遇到了返回事件绑定在小图标上导致实机点击区域不稳定的问题，用扩大点击容器和统一公共返回组件的方法解决。
- 实现了个人中心和编辑资料相关页面的新拟态样式统一，通过在个人中心信息卡、资料编辑卡、密码修改表单中复用 Theme 设计令牌、圆角卡片、柔和阴影、胶囊按钮和浅灰输入框实现。
- 实现了课表日期星期对齐和空教室默认按当前周查询，通过将学期开始日期归一到所在周周一，并让课表页与空教室页复用统一周次计算工具实现。遇到了课表页和空教室页分别计算周次导致查询条件不一致的问题，用共享日期工具统一周次和日期计算的方法解决。
- 实现了空教室查询与课表教室占用匹配，通过后端同时比较教室号和教学楼加教室号的归一化名称实现。遇到了课表中教室可能保存为"教学楼+教室号"、教室表只保存教室号导致占用教室仍被返回的问题，用统一去空格并双格式匹配的方法解决。
- 实现了课表页日期兜底显示和切回本周按钮尺寸位置调整，通过在学期开始日期未加载时按今日日期和当前周反推周日期，并将按钮改为固定宽度右边界定位实现。遇到了接口数据未到达时日期退化为序号的问题，用本地日期兜底计算的方法解决。
- 实现了鸿蒙前端美化交互原型页面，通过单文件 HTML+CSS+JS 构建手机壳预览，含 5 个底部 Tab（首页/二手/社团/消息/我的）和 8 个核心子页面（商品详情/聊天/课表/空教室/失物招领/设置），所有组件统一使用 Theme.ets 设计令牌（颜色/圆角/阴影/间距/字号），支持 Tab 切换、页面导航栈、发送消息、收藏切换、筛选过滤等交互。遇到原型需在浏览器预览的问题，用 python3 http.server 本地服务的方法解决。
- 增强了 Theme.ets 设计令牌，新增字号阶梯（fsDisplay/fsTitle/fsSubtitle/fsBody/fsCaption/fsSmall）和圆角语义别名（cardRadius/heroRadius/chipRadius），为后续全站页面统一美化奠定基础。
- 修复了DevEco Studio 26提示"工程构建配置需要升级"的问题，通过在hvigor/hvigor-config.json5中添加@ohos/hvigor-ohos-plugin和@ohos/hvigor依赖（版本6.26.1），并在用户目录创建.npmrc配置HarmonyOS npm镜像仓库实现。遇到了hvigor插件未安装和.npmrc缺失导致依赖下载失败的问题，用补全hvigor-config.json5依赖声明和配置repo.harmonyos.com镜像地址的方法解决。
- 实现了首页快捷入口卡片色调统一和今日课表/我的报名等高对齐，通过将空教室、社团互动、AI助手三张快捷卡的颜色从各自语义色（success/secondary/danger）统一改为 Theme.primary 主色+primaryLight 浅底，并为两个并列卡片的父级 Column 添加 .height('100%') 强制拉伸到 Row 高度实现底边对齐。遇到了 constraintSize({ minHeight: 200 }) 只能设下限无法真正等高的 ArkUI Row 子元素不对齐问题，用 .height('100%') 拉伸的方法解决。遇到了 .height('100%') 导致 Row 高度撑满 Scroll 造成校园公告上方大片空隙的问题，改用 constraintSize({ minHeight: 200 }) 自然高度方案解决。遇到了 hero 紫色卡片和校园公告卡片 .width('100%') + .margin({ left: 18, right: 18 }) 总宽度超出屏幕的问题，用去掉 .width('100%') 保留 margin 自动约束的方法解决。
- 实现了首页视觉统一优化：1）今日课表和我的报名标题并入卡片内，与校园公告格式一致，通过将 backgroundColor/borderRadius/shadow 从内层内容 Column 提升到外层 Column 实现；2）失物招领大卡从橙黄渐变改为 primaryLight 浅靛蓝底色，角标同步从橙色改为靛蓝色系；3）课表/空教室/社团互动/AI助手四个快捷入口卡片从靛蓝浅底改为白色背景+阴影，与界面整体白底风格统一。
- 实现了首页图标和标题字体的视觉优化：1）将课表/空教室/社团互动/AI助手四个快捷入口的 SVG 图标分别替换为 emoji（📅/🏫/🎭/🤖）字号 26，提升图标辨识度；2）将今日课表和我的报名两个卡片的标题字号从 13 调整为 16，与校园公告标题字号统一。
- 修正了今日课表"完整课表"跳转链接的箭头字符不统一问题，通过将 "完整课表 →" 改为 "完整课表 >"，与我的报名/校园公告的 "更多 >" 箭头字符统一。
- 实现了首页玻璃拟态（Glassmorphism）视觉效果，通过将首页 Scroll 区域底层添加紫蓝粉渐变背景（#667eea → #A855F7 → #F472B6），并将所有白色卡片（快捷入口×4、失物招领、今日课表、我的报名、校园公告）的背景改为半透明 rgba(255,255,255,0.65) + backdropBlur(16) 毛玻璃模糊 + 半透明白色细边框 rgba(255,255,255,0.4) 实现。保留了 hero 渐变大卡的原色作为视觉焦点，其他卡片透出底层渐变色彩形成透明玻璃感。
- 实现了首页从玻璃拟态全面切换为新拟态（Neumorphism）设计风格，通过以下改动实现：1）Theme.ets 设计令牌全面替换——背景色 bgPage/bgCard 改为 #E0E5EC 同色无边界设计、文字色 textPrimary 改为 #4A5568 柔和深灰、圆角全面增大（radiusMd:20/radiusLg:24）、新增 nmRaised/nmPressed 右下暗阴影（#A3B1C6）模拟浮雕凸起；2）MainPage.ets 首页移除紫蓝粉渐变背景和所有 backdropBlur 毛玻璃效果，Hero 大卡去掉渐变改用纯色 bgCard + nmRaised 阴影、4 个快捷入口卡片从玻璃透明改为新拟态凸起、失物招领/今日课表/我的报名/校园公告 7 个卡片统一改为 bgCard 同色背景 + nmRaised 大阴影浮起效果；3）Hero 卡内统计数字和文字从白色改为 Theme.textPrimary/textTertiary 柔和深色体系。通过将卡片背景色与页面背景色统一、仅通过右下暗阴影制造浮起感的 neumorphism 技法实现。
- 强化了新拟态浮雕效果以接近 AI 概念图标准，通过以下改动实现：1）Theme.ets 阴影系统全面增强——nmRaised 阴影 radius 20→30、offset 5→8、color 透明度 0x80→0xA0（更暗更明显），新增 nmRaisedLg 阴影（radius:40/offset:12/color:#A3B1C6B0）专用于 Hero 大卡；2）圆角全面加大——radiusSm 10→14、radiusMd 20→24、radiusLg 24→32、radiusPill 26→30 让卡片更圆润；3）新增新拟态渐变专用色 nmSurface(#EAEEF3 高光色) 和 nmShade(#C8CFDA 阴影色)；4）MainPage.ets 首页 8 个关键卡片（Hero、4 快捷入口、失物招领、今日课表、我的报名、校园公告）将 backgroundColor 替换为 inline 写死的 linearGradient（角度 135，颜色 [['#EAEEF3', 0.0], ['#E0E5EC', 0.5], ['#C8CFDA', 1.0]]），从左上高光到右下阴影模拟真实光源，解决鸿蒙 ArkUI 单层阴影无法实现双阴影浮雕的痛点。遇到老板反馈"实现效果与 AI 概念图差距大"的问题，用"linearGradient 模拟光源 + 增强右下阴影 + 加大圆角"组合方案让浮起感更接近设计稿。\n- 二次加强新拟态浮雕立体感以解决"背景偏暗、卡片浮起感弱"问题，通过以下改动实现：1）Theme.ets 背景色从 #E0E5EC 改为 #ECF0F5（更白更亮，让阴影更突出），新增 nmShadow(#8B9BB4 更深暗影) 和 nmHighlight(#FFFFFF 高光) 专用色，渐变色 nmSurface 从 #EAEEF3 改为 #FAFCFE（更白高光），nmShade 从 #C8CFDA 改为 #B8C1D0（更深阴影）；2）nmRaised 阴影重调：radius 30→20、offset 8→10、color 透明度 0xA0→0xC0 且颜色改为 #8B9BB4（更深更聚焦的右下投影，浮起感更强），nmRaisedLg 同步加强（offset 12→14、color #8B9BB4D0），sm/md/lg 普通阴影也用更深的 #8B9BB4 色；3）MainPage.ets 首页 8 个卡片渐变全部从 ['#EAEEF3', 0.0], ['#E0E5EC', 0.5], ['#C8CFDA', 1.0] 替换为更白更深的 ['#FAFCFE', 0.0], ['#ECF0F5', 0.5], ['#B8C1D0', 1.0]，让卡片内对比度更强、浮雕感更明显。遇到老板反馈"背景还是不够白、立体感小"的问题，用"页面背景变白 + 卡片渐变对比拉大 + 阴影颜色变深"组合方案让浮起效果接近设计稿。\n- 重构新拟态阴影策略以解决"阴影在卡片内部看起来很脏"问题，通过以下改动实现：1）MainPage.ets 首页 8 个卡片的 linearGradient 内部渐变全部去掉，改为纯 backgroundColor(Theme.bgCard)，让卡片本体干净同色，不再有内部脏色；2）nmRaised 外部阴影大幅增强——radius 20→30、offset 10→12、color #8B9BB4C0→#8B9BB4D0，nmRaisedLg 同步拉满（radius 30→40、offset 14→16、color #8B9BB4D0→#8B9BB4E0），浮雕凸起完全靠外部投影实现。通过"卡片本体纯色统一 + 外部投影拉满"新拟态正确技法实现。
- 将新拟态配色系统切换为纯白色调，通过以下改动实现：1）Theme.ets 中 bgPage 从 #ECF0F5 改为 #FFFFFF、bgCard 改为 #FFFFFF、bgInput 改为 #F4F5F7、bgHover 改为 #F0F1F3，实现纯白背景 + 纯白卡片的极致干净画面；2）纯白卡片在纯白背景上仅通过外部阴影（nmRaised/nmRaisedLg 的 #8B9BB4 投影）来制造分层感和立体感，凸起效果在白色底上更加明显。通过"纯白统一底色 + 强外部投影"技法实现。\n- 通过 Stack 双层嵌套技术实现新拟态卡片的真实 3px 厚度感，解决"卡片很薄、立体感不足"问题：1）Theme.ets 新增 nmThickness（#EBEDF1 厚度底层色，比背景深一档），nmRaised 阴影调整为 radius:18 offset:{4,6} color:#8B9BB4D0（与底层厚度对齐的精准投影），nmRaisedLg 调整为 radius:28 offset:{8,10} color:#8B9BB4E0；2）MainPage.ets 首页 4 个快捷入口全部改造为 Stack 双层结构 —— 外层 Stack 包裹两层 Column，底层 Column 用 Theme.nmThickness 偏深灰色 + 圆角 + offset({x:3, y:3}) 向右下偏移模拟"厚度边"，上层 Column 用 Theme.bgCard 纯白 + 1px Theme.nmThickness 浅色边框 + 圆角作为卡片本体，整体 Stack 加 shadow(ShadowStyle.nmRaised) 制造浮起感。通过"Stack 双层错位 + 浅色描边 + 精准外部投影"组合方案实现老板参考图中的"卡片有厚度"效果（卡片上半边高光、右下半边双线、阴影自然）。
- 重构新拟态层次感方案，解决"纯白底+纯白卡片效果差、实机渲染差距太大"的问题，通过以下改动实现：1）Theme.ets 背景色 bgPage 从纯白 #FFFFFF 回调到柔和灰白 #F0F3F6，利用色差自然衬托白色卡片——这是新拟态的核心技法（背景略深于卡片，层次自然出现）；2）移除 nmThickness、nmSurface、nmShade 等不再使用的辅助色，简化设计令牌；3）阴影系统全面重调——所有阴影 offsetX 设为 0（正投下阴影更干净），nmRaised radius:18→12 offsetY:6→4 color 透明度 D0→90（更轻盈自然），nmRaisedLg radius:28→20 offsetY:10→6（大卡片阴影收敛），避免之前重阴影 + 纯白背景导致的"脏感"；4）MainPage.ets 首页 4 个快捷入口从 Stack 双层结构回退为简洁单层 Column（白色背景 + 圆角 + 轻阴影），解决之前偏移错位在实机上看起来像渲染 bug 而非立体厚度的问题；5）今日课表和我的报名列表项补充 shadow(ShadowStyle.sm)，保证灰白背景上每条内容都有轻微浮起。通过"背景微灰衬托白卡 + 轻阴影 + 单层卡片"组合方案实现干净自然的新拟态效果。
- 重构底部 Tab 栏布局并将课表独立为子页面，通过以下改动实现：1）Tab 栏从「首页/课表/交易/AI/我的」改为「首页/交易/消息/AI/我的」——Tab 1 从 ScheduleContent 改为 MarketContent、Tab 2 从 MarketContent 改为新增 MessageContent（轻量占位 Column）并注册 ic_message/ic_message_active 新图标（聊天气泡 SVG 风格与现有图标统一，非激活#9CA3AF/激活#4F46E5）；2）消息 Tab 点击时在 onChange 中通过 router.pushUrl 跳转到 ConversationListPage，不实际占据 tab 展示区域，实现"点击即跳转"的轻 Tab 体验；3）首页"我的课表"快捷入口从 this.currentTab=1（切到课表 Tab）改为 router.pushUrl({ url:'pages/SchedulePage' }) 独立页面跳转，课表不再作为 Tab 嵌入 MainPage，而是在已有 SchedulePage.ets 中独立渲染（含学期加载、周选择器、7x10 课表网格、课程详情弹窗等完整功能）；4）保留了 MainPage 内 ScheduleContent/ScheduleHeader/ScheduleGrid 等 Builder（避免连锁删除其他页面的依赖），仅在 Tab 级别解除挂载。通过在 Tab onChange 中检测 index===2 触发页面跳转的技法实现消息入口。
- 将首页 4 个快捷入口按钮改造为新拟态双层按钮风格（参考图效果），通过以下改动实现：1）创建 QuickEntry @Builder 复用方法，接收 emoji/label/targetPage 三个参数；2）每个按钮使用 Stack 双层结构——底层 Column 用 #C8CFDA 深灰色 + borderRadius(28) + offset({x:4,y:4}) 向右下偏移模拟按钮"厚度边"，上层 Column 用 linearGradient（angle:180，#FFFFFF→#F4F5F7）模拟顶部柔和高光效果，整体 Stack 设置 clip(false) 允许偏移超出；3）图标从 SVG Image 改为大号 Emoji 文字（📅📅/🏫/🎉/🔍），fontSize 32，视觉更醒目；4）按钮尺寸从 64×72 增大到 68×88（Stack 容器 72×92），圆角从 24 增至 28，外部阴影用柔和投影（radius:14, offsetY:5, #A3B1C670）。通过"底部厚度暗色层 + 顶部渐变高光 + 柔和弥散投影"组合方案实现新拟态凸起按钮效果。遇到了两个问题：1）Stack 外层无 borderRadius 导致阴影按直角矩形扩散，看起来像方框，用给 Stack 添加 borderRadius(28) 的方法解决；2）8 位 hex 阴影色（#A3B1C670 及 ShadowStyle.md 的 #A3B1C680）在 ArkUI 下均被解析为 #AARRGGBB 格式，蓝色通道只剩 70-80 导致泛黄，最终改用 6 位 hex 纯中性灰 #ABB3C0（不包含 alpha 通道，无格式歧义，RGB 三通道均衡无色彩偏向），radius 增至 16 补偿透明度损失。3）顶层渐变方向调试：根据 ArkUI 官方文档，GradientDirection.RightBottom = 左上→右下（#0% 在左上），对应 angle: 45°。`angle: 135` 改回 `direction: GradientDirection.RightBottom` 枚举写法，更明确（避免 angle 数字歧义）。新拟态立体感主要由"投影位置 + 厚度层偏移 + 圆角层叠"三要素构成，渐变高光是辅助。最初用 #FFFFFF → #E4E8EE 对比度太小（差异仅 17）看不出高光，加深到 #FFFFFF → #D0D5DD（差异 47）后左上半边的高光感明显增强，与右下半边厚度+阴影形成完整的"光从左上斜照"浮雕效果。4）解决"右上左下白边凸出"问题：根因是底层 Column（68×88, offset(4,4)）只覆盖 Stack 的中间区域，Stack 圆角"右上"和"左下"两个角落（约 4×4 px）没有 Column 覆盖，透出页面背景 #F0F3F6（与上层 Column 圆角边缘 #FFFFFF 纯白存在明显对比）形成"白边凸出"。修复方案迭代：v1 在 Stack 外层包 Column + backgroundColor('#F0F3F6')，仍有白边（页面背景与上层圆角边缘颜色不一致）；v2 直接给 Stack 自身设 linearGradient，direction: GradientDirection.RightBottom, colors: [['#FFFFFF', 0.0], ['#D0D5DD', 1.0]]（与上层 Column 渐变方向和颜色完全一致）。这样圆角空白区域显示 Stack 自身的渐变色，与上层 Column 圆角边缘的渐变色精确对齐（相同位置相同颜色），实现无缝融合，不再有"白边凸出"。同时简化代码，去掉外层 Column 包装。5）回滚纯白方案，恢复新拟态立体效果：底层厚度层 Column 用 #C8CFDA，上层 Column 和 Stack 均用 GradientDirection.RightBottom 对角渐变（#FFFFFF → #D0D5DD），配合右下阴影（shadow radius:16, color:#ABB3C0, offsetY:4），保持"光从左上斜照"的浮雕立体感。6）上层内容 Column 改为纯白色 backgroundColor('#FFFFFF')：去除渐变，与页面底色 #F0F3F6 形成明确区分，底部厚度层 #C8CFDA + Stack 渐变保留，立体感和层次感不变。7）登录页和注册页按钮统一改为新拟态风格：将 LoginPage 和 RegisterPage 中原有的 Button（蓝色背景、白色文字）替换为 Stack 双层结构——底层 Column 填满 Stack 区域（width/height 100%）用 #C8CFDA 做厚度，上层 Column 用 #FFFFFF 纯白 + margin({right:4, bottom:4}) 露出右下厚度边，Stack 外层 linearGradient（#FFFFFF → #D0D5DD）+ borderRadius(28) + shadow 与首页快捷入口按钮风格完全一致。登录按钮宽度 85%，注册按钮宽度 100%，高度统一 52（48+4 厚度边），fontColor 从白色改为 Theme.textPrimary 深色文字。通过 margin 方式（而非 offset）实现全宽自适应按钮的厚度露出效果。8）输入框和选择器统一改为新拟态风格：LoginPage 的学号/密码输入框、RegisterPage 的学号/姓名/密码/确认密码/手机号输入框以及 SelectField 选择器组件（年级/学院/专业/班级/籍贯）全部用相同 Stack 双层结构包裹——底层 Column #C8CFDA 厚度 + 上层 TextInput/Row #FFFFFF 纯白 + margin({right:4, bottom:4}) 露出右下厚度边，外层 linearGradient + borderRadius(28) + shadow 与按钮风格统一。去除原有 Theme.bgInput 背景色、borderRadius(8)、shadow(ShadowStyle.sm)，SelectField 的 margin({top:12}) 从内层 Row 移到外层 Stack 保持间距一致。9）首页顶部问候区拆分为两个独立新拟态组件：将原先一整块新拟态卡片拆分为——左侧文字卡片（"你好，XX" + "college · major"）用 auto 宽度（去掉 layoutWeight(1)）+ height(73) 的新拟态 Stack 卡片（#C8CFDA 厚度 + #FFFFFF 纯白上层 + linearGradient + shadow）；右侧头像从卡片内提取为独立 56×56 新拟态圆形 Stack（背景纯白 #FFFFFF 做"白色 cushion/ring"、底层 #C8CFDA 40×40 圆 offset(3,3) + 上层 40×40 头像/首字母）。两者用 Row + VerticalAlign.Center 水平排列，Blank() 弹性填充中间间距。文字卡片 padding({left:28, right:24, top:16, bottom:14}) 大幅增加左侧留白（之前是 20）。圆形头像 shadow radius 大幅减小至 8（之前 12），offsetY 从 4 减到 3，整体阴影范围更紧凑，避免视觉上"拉出去太大"。文字卡片 shadow radius 同步从 16 减到 10，offsetY 从 4 减到 3，统一收敛阴影强度。
- 修复了首页问候卡片新拟态效果丢失问题，通过给外层文字卡片 Stack 加 .layoutWeight(1) 显式占满 Row 剩余空间，shadow 从手写 {radius:10, color:'#ABB3C0'} 替换为 Theme.ShadowStyle.nmRaised（radius:12, color:'#A3B1C690', offsetY:4），头像 Stack shadow 同步升级。遇到了 Stack 没显式 width 但内部 Column 写 width('100%').height('100%') 导致父子循环依赖、Stack 算不出尺寸、厚度层和阴影和 linearGradient 全部渲染异常的问题，用 .layoutWeight(1) 让父容器获得确定尺寸再用 100% 拉伸子元素的方法解决。
- 修复了问候区三处细节问题：1）问候文字卡片和下方课表快捷入口竖直不对齐——原因是 QuickEntry Row 用了 SpaceAround 在第一个元素前有额外间距，改成 SpaceBetween 让第一个元素左对齐；2）文字卡片右下阴影太粗——厚度层 margin 从 4px 缩到 2px；3）头像阴影看起来加在头像上而非白色底上——原因是内部厚度层 offset(3,3) 超出白色 Stack 边界，加 .clip(true) 裁切让 shadow 紧贴白色底。
- 简化问候区新拟态结构：去掉问候卡片和头像的 #C8CFDA 灰色厚度层（Stack 双层结构），改为纯白色图层 + ShadowStyle.nmRaised 阴影，shadow 原点紧贴白色底；问候卡片 Column 加 margin({left:8}) 微调与下方课表按钮对齐；Row 从 margin 改为 padding 统一左右间距；QuickEntry Row 恢复 SpaceAround 保持与左边留出距离。
- 给问候卡片和头像增加立体感：用极淡表面渐变（linearGradient #FFFFFF→#F2F4F8，左上→右下模拟光源）替代纯白背景，配合加强阴影（卡片 radius:14/offsetY:6，头像 radius:12/offsetY:5），通过"表面光源渐变 + 右下方浮起投影"两层叠加实现无厚度层的新拟态立体效果。
- 问候卡片和头像立体感照搬 QuickEntry 双层 Stack 结构：底层 #C8CFDA 厚度层 offset(4,4) 露出右下厚度边，上层 #FFFFFF 纯白内容，外层 Stack linearGradient(#FFFFFF→#D0D5DD) + clip(false) + shadow(radius:16/#ABB3C0/offsetY:4)，参数完全一致。
- 修复问候卡片和头像重叠问题：去掉 Stack 的 layoutWeight(1)，让卡片按内容宽度自适应不占满 Row；厚度层改为 Stack 自身 backgroundColor('#C8CFDA') + 内部白色 Column margin({right:4, bottom:4}) 露出右下灰色底实现（替代 offset Column）。修复头像阴影位置问题：去掉内部 offset 厚度 Column，Stack 自身白色底 + linearGradient + shadow，shadow 原点在白色外层。
- 头像左移 + 增强立体感：头像 Stack 加 margin({right:8}) 向左收，内部补回 offset Column(#C8CFDA, offset(4,4)) 厚度层（与 QuickEntry 结构一致），shadow 原点仍在白色外层 Stack 上。
- 头像修复：去掉 Stack 内部 offset(4,4) 灰色厚度 Column，shadow 现在明确加在白色外圆边缘上；margin right 从 8 调至 12 让头像更左移；问候卡片未动，左侧仍与课表对齐。
- 今日课程和我的活动各加白色卡片容器：每个 Column 加 backgroundColor('#FFFFFF') + borderRadius(16)，标题 padding 加 top:16 让文字不贴顶，两卡片间用 Blank().width(12) 留间距。
- 今日课程和我的活动从左右并排改为上下放置：外层 Row 改 Column，卡片 layoutWeight(1) 改 width('100%')，Blank width 12 改 height 12。
- 课程卡片和活动卡片缩小框框：padding 从 {left:12, right:12, top:6, bottom:6} 收至 {left:10, right:10, top:4, bottom:4}，margin bottom 从 6 缩至 4。
- 取消今日课程和我的活动大卡片白色背景（去 backgroundColor('#FFFFFF') 和 borderRadius(16)），每个小 item 用 Stack 双层结构增加立体感：底层 #C8CFDA Column offset(3,3) 露出厚度，上层 #FFFFFF Row 纯白内容，外层 Stack linearGradient(#FFFFFF→#D0D5DD) + clip(false) + shadow(radius:12/#ABB3C0/offsetY:3)，与问候区/快捷入口结构一致。
- 修复课程/活动 item 灰色厚度层撑满整 Stack 的 bug：之前用 inner Column width('100%').height('100%') offset(2,2) 方式，动态高度场景下 height('100%') 会撑爆。改为外层 Stack 自己设 backgroundColor('#C8CFDA') 当厚度色，内层白色 Row 用 margin({right:2, bottom:2}) 暴露右下 2px 灰色边（与问候卡片同款结构）。遇到了动态高度 Stack 内部 child 不能用 height('100%') 否则会撑满父容器可用空间的问题，用外层背景色+内层 margin 的方式解决。
- 统一立体元素圆角：课程和活动 item 的 borderRadius 从 8 调到 16（内外两层 Stack 都改），与问候卡片/快捷入口的 28 风格一致，更圆润。
